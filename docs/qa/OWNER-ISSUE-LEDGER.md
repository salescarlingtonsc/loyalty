# Owner issue ledger

Last consolidated: 2026-08-03

This ledger makes every owner complaint durable and prevents a screenshot from
being treated as an isolated one-off. Detailed surface and test mappings live
in `TRACEABILITY-MATRIX.md`.

## Lifecycle

| State | Meaning |
| --- | --- |
| `CAPTURED` | Requirement/symptom is recorded but not yet reproduced. |
| `REPRODUCED` | The symptom is observed with named fixture and evidence. |
| `IMPLEMENTED_UNVERIFIED` | Code exists, but the required acceptance evidence is incomplete. |
| `VERIFIED_LOCAL` | Relevant local automated tests pass. This is not browser, database, or production proof. |
| `VERIFIED_BROWSER` | Required real-browser journeys and viewports pass with artifacts. |
| `VERIFIED_DATABASE` | Persistent records and cross-surface projections are verified against the target database environment. |
| `VERIFIED_PRODUCTION` | The exact accepted version is verified in production after authorized release. |
| `DEFERRED_OWNER` | Owner intentionally deferred it; reason and date are recorded. |
| `BLOCKED` | A named external input or authority is required. |
| `SUPERSEDED` | A later owner decision replaced it; the replacement ID is recorded. |
| `CLOSED` | Every acceptance criterion has the required evidence and independent review. |

Do not skip directly from `CAPTURED` or `IMPLEMENTED_UNVERIFIED` to `CLOSED`.

## Evidence required in every issue

- exact complaint and source date/context;
- affected role, branch, sector, module, and device;
- realistic fixture;
- reproduction artifact;
- regression test name;
- owner -> staff -> customer evidence where data crosses surfaces;
- disabled/empty/denied/retry/refresh/mobile evidence when applicable;
- independent reviewer and verdict;
- production evidence only after an authorized release.

## Consolidated issue register

The source descriptions below consolidate the owner's cumulative instructions
and screenshots through 2026-07-31. `VERIFIED_LOCAL` means only the named local
phase has evidence; it is intentionally not marked closed.

| ID | Owner complaint / acceptance intent | Current state | Required proof before close |
| --- | --- | --- | --- |
| `UX-SIMPLIFICATION-001` | Owner annotated iPad screenshots on 2026-08-03 plus follow-up Dashboard screenshot: Peekaa is too complicated for ordinary SME owners because operational actions, historical records, analytics, accounting and setup are mixed together. Simplify visible navigation and the Dashboard, Customers, Record Sale, Appointments, Waitlist, Sales, Reports, P&L, Expenses and Services pages so each page has one clear purpose, restrained colour, tablet-safe layout, clear primary actions, and no misleading dashboard or ledger metrics. Latest Dashboard screenshot additionally requires removing the large Dashboard intro, moving date controls to the compact header/performance row, removing “How Cubbly is doing”, keeping only Valid visits/Revenue/New customer members/Inactive customers, and regrouping charts under **Understand your business**. | `VERIFIED_LOCAL` | Before close, production must show the V150 build on `/business` routes with the simplified sidebar, compact Dashboard, four KPI cards only, mutually exclusive customer inactivity cards, subtly highlighted found-customer panel, Calendar/List appointment segmentation with current-time line, Waitlist badge, Sales ledger without Quick sale, Reports labels, P&L hierarchy, Expenses list/add segmentation, Services/Bundles catalogue-first segmentation and no Services Resources card. Evidence must include targeted V150/V141/V142/V143/V148 automated tests, production build, deployment ID, production build identity, desktop and tablet smoke checks, and confirmation that sale, reversal, credit, loyalty, ledger, expense and P&L semantics were not changed. |
| `UX-POLISH-001` | Owner V152 instruction on 2026-08-03: keep the V150/V151 architecture as the baseline and polish the commercial SaaS feel only. Improve consistency, mobile refinement, empty states, skeleton loading, subtle transitions, table/button/form feedback, accessibility and directly related redundant UI re-renders across business pages without redesigning modules, moving workflows, changing query libraries, changing caching strategy, or altering financial/loyalty/ledger/appointment/billing calculations. | `IMPLEMENTED_UNVERIFIED` | Before close, production must show shared spacing/radius/button/input/table/empty/loading patterns on Dashboard, Customers, Record Sale, Appointments, Bookings, Waitlist, Sales, Reports, P&L, Expenses, Services, Packages, Branches, Staff and Settings. Empty states must explain what happened and what to do next; loading states must use skeletons; mobile phone widths must avoid horizontal page scrolling, clipped controls and oversized sticky UI; repeated form submissions must be guarded where touched. Evidence must include targeted V152 tests, production build, deployment ID, production build identity, production smoke checks and confirmation that no financial, loyalty, reversal, ledger, expense, P&L, appointment or billing semantics changed. |
| `MERCHANT-INSIGHTS-001` | Owner V153 instruction on 2026-08-03: make Peekaa a practical business assistant by showing deterministic merchant insights, retention readiness, preparation-only campaigns, and a precise multi-branch capability review without changing sale, ledger, loyalty, credit, package, expense, P&L, appointment, billing, authentication, tenant or permission semantics. | `CLOSED` | Production Dashboard shows deterministic Merchant Insights based on existing dashboard/customer data, zero-base-safe revenue comparisons, 60+ inactive-customer recommendations, busiest-day insight only when data is sufficient, a Business Health Summary rather than a subjective score, and scope labels. Customers shows mutually exclusive retention readiness groups with view/export/prepare-campaign actions and no live Send action. Repository evidence includes V153 insight-rule docs, multi-branch readiness score and explicit unsupported-capability list, targeted automated tests, production build, deployment ID, production build identity, production smoke checks and confirmation that no protected business semantics changed. Evidence: `tests/business-ui/v153-merchant-insights.test.mjs` 7/7; V150-V153 targeted regressions 32/32; `npm run quality`; `npm run build`; `git diff --check`; production `/api/build`; production route smoke checks for `/`, `/business`, `/admin`, `/privacy.html`, `/terms.html`. |
| `LAUNCH-FREEZE-001` | Owner decision 2026-08-03: freeze the launch product. Add no features, modules, dashboards, or workflows. Audit the existing implementation for correctness, remove bugs and misleading metrics, improve stability and performance, ensure every dashboard value is accurate, and remove all placeholder behavior. An already-scoped incomplete feature must either be completed within its existing workflow or hidden until production-ready. Reliability and user trust take priority over scope growth. Independent review additionally reproduced launch-trust defects on 2026-08-03: a null or blank expense category can make P&L aggregation fail; Grow exposes an unfinished low-level image-storage control; branch-limited staff can receive business-wide current customer credit through SECURITY DEFINER Dashboard/Reports aggregates; and launch reconciliation attributes a system-created safety pause to an arbitrary owner. The first fresh 176-migration replay also reproduced a V145 migration failure: `sale_commission` inserted `counts_as_revenue` where the established trailing `flat_cents` column already exists, which PostgreSQL rejects and which would otherwise risk losing flat-commission semantics. | `VERIFIED_DATABASE` | The reconciled V145 candidate corrects valid-visit, visiting-customer, branch-points, complete-date, Singapore-calendar, P&L completeness/FX/month, staff revenue treatment, customer-balance, expiry, waitlist and mixed-scope contracts. P&L returns the full converted expense total and groups null/blank labels under **Uncategorised** without rewriting the expense. Grow exposes no storage-path control or technical contract copy, preserves an existing `image_ref` on save, and keeps published images customer-visible. A fresh disposable database applies all 176 canonical files while `sale_commission` retains its original ten columns and flat-over-percentage arithmetic, then appends `counts_as_revenue` as column eleven. A branch-limited user receives `null` plus an explicit unavailable scope for business-wide credit—never zero or another branch's amount—while a whole-business-authorized owner receives the exact signed total. Migration-created safety pauses/audits use the documented system sentinel rendered as **Peekaa launch safety**; owner-created pauses retain the real actor. Launch UI is absent for unready Stored value, advanced-rule authoring, unavailable WhatsApp OTP, ineffective inbox quiet hours and unreconcilable item-mix claims. V144's prospective non-refundable policy remains intact and the shipped promotion helper is described as template-assisted, not generative AI. Current evidence: focused **37/37** and combined **55/55**, fresh **176/176** replay, V144/V145 rollback suites with zero residue, desktop/390px Chrome acceptance, strict 313-control inventory, and complete **1,490/1,490** validation plus six-page build. Sol independently accepted the frozen candidate on 2026-08-03 with no P0/P1/P2 release blockers. Commit `addb6f0` is pushed; V144 was already exact in production; V145 was applied as production migration `20260803050954`; and deployment `dpl_BX8Ahv9Hk3WHhJmyNUY79UnHLqiw` is `READY` with the accepted live index hash and exact production build identity. Post-deploy routes, headers, desktop/390px rendering and runtime-error checks pass. This row remains `VERIFIED_DATABASE`, not `CLOSED`, because authenticated owner/staff/customer target proof is incomplete and the live annual/monthly Stripe catalogue is empty, leaving owner signup safely blocked before Checkout. Evidence: `V145-LAUNCH-FREEZE-AUDIT.md`. |
| `AUTH-001` | Customer signup must work by Singapore mobile; normal login uses password/passkey, OTP only for account creation and forgot password; password reset, reveal controls, back navigation, and passkey placement must work. | `IMPLEMENTED_UNVERIFIED` | iPhone/Safari and Chrome journeys; one-OTP accounting; reset persistence; unsupported-passkey state. |
| `AUTH-FORWARD-001` | Owner ruling 2026-07-30: historical test logins are disposable and must not be used as acceptance evidence. Prove only forward identity and database behavior with a newly created synthetic customer: one OTP during account creation, zero OTPs during the subsequent phone/password login, then current approval, role, module, branch, relationship, and persistence checks. | `VERIFIED_DATABASE` | Historical logins are excluded. Fresh password sessions for the forward-created `CUS-NEW` customer and Orchard front desk drove the rendered customer -> staff -> customer chain: 1,080 points and an eligible reward, pending QR with no spend, server-confirmed exact-branch redemption, then 80 points and a newest `-1000` History event after customer sign-in/refresh at desktop and 390px. The same completed intent and canonical operation were verified in local Postgres. The full gate passes 1,284/1,284 and exact evidence is indexed in `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Provider-side OTP accounting and native-device passkey behavior remain under `AUTH-001`; independent review remains before `CLOSED`. |
| `JOIN-001` | A business QR must remain active when signups are enabled; scan -> signup/sign-in -> automatic one-time business link. No customer search/self-link. | `IMPLEMENTED_UNVERIFIED` | New and existing customer browser runs, duplicate scan idempotency, disabled-link state, DB relationship. |
| `CUSTOMER-001` | Default customer home, compact logo programme selector, individual programme overview, profile menu, one notification action, English-only customer UI. | `IMPLEMENTED_UNVERIFIED` | Desktop/mobile visual artifacts using 0, 1, and 5+ programmes; keyboard/touch navigation. |
| `CUSTOMER-002` | Customer sees complete transactions, visits, points per transaction, reversals, packages, vouchers, and balances consistent with business records. | `IMPLEMENTED_UNVERIFIED` | Ledger-to-portal database comparison including reversal/retry/reload. |
| `CUSTUX-001` | Screenshot 2026-07-29: remove the confusing destructive Disconnect button and audit every customer control so its purpose is immediately understandable. | `VERIFIED_LOCAL` | v103 control inventory and DOM regressions prove Disconnect, duplicate Add programme, presentation Retry, and programme-level notification preferences are absent. Authenticated desktop/mobile browser artifacts remain required before close. |
| `CUSTUX-002` | Screenshot 2026-07-29: a linked programme shows "This programme could not be loaded" even while balance/rewards/history appear. Find the exact failing contract and provide a coherent recoverable experience. | `VERIFIED_LOCAL` | Root reproduced as omitted linked business UUID; v103 summary migration/rollback SQL and UI tests prove UUID projection, caller validation, linked/outsider/cross-firm denial, and optional-presentation fallback. Target reload evidence remains required before close. |
| `CUSTUX-003` | Screenshot 2026-07-29: 70,576 balance dominates the page, the balance is duplicated, and an empty Tier progress card says "Every visit still counts" twice. | `VERIFIED_LOCAL` | v103 tests use 70,576 and ordinary balances and prove one compact formatted balance, no duplicate points metric, and no tier surface without real progress. Authenticated 390px/desktop visual artifacts remain required before close. |
| `CUSTUX-004` | Screenshot 2026-07-29: Transactions & points should be supporting content under one History section rather than a competing programme block. | `VERIFIED_LOCAL` | v103 DOM tests prove one initially collapsed accessible History disclosure containing transactions and loyalty activity; empty success has no false Retry/Refresh. Authenticated mobile navigation remains required before close. |
| `CUSTUX-005` | Screenshot 2026-07-29: every eligible reward needs a clear "Redeem now" action beside it that prepares the customer QR. | `VERIFIED_LOCAL` | v89/v103 regressions prove eligible classic/catalog rewards show Redeem now and create idempotent pending intents; disabled/insufficient/ended states have no dead CTA. Target customer-to-staff scan evidence remains required before close. |
| `CUSTPROFILE-001` | Nestly Trial & Test 2026-07-30, reported 2026-08-01: remove the visible “Gender: Not set” noise; explain Loyalty in ordinary language; rename customer Timeline to Sales history. | `VERIFIED_BROWSER` | V129 removed gender from add/import/list/profile and, at that release boundary, its dashboard chart; it presents plain-language Rewards with advanced correction collapsed and renames the unified feed **Sales history**. V141 later introduced a separate aggregate recorded-gender demographic chart on Dashboard; it does not restore gender as a customer-profile field or requirement. The actual production `clientDetail` renderer passes at 1440px/390px with no page error or body overflow. Sol independently accepted the corrected V129 candidate with no P0-P3 findings. Evidence: `V129-TRIAL-TEST-UX.md`. Target proof remains before `CLOSED`. |
| `CUSTINACTIVE-001` | Nestly Trial & Test 2026-07-30, reported 2026-08-01: Customers must show people inactive for 30, 60, or 90 days. | `VERIFIED_DATABASE` | V129 adds an authenticated Clients-read RPC with inclusive Singapore-calendar 30/60/90 boundaries, bounded pagination, last valid visit and Never visited. Disposable Postgres acceptance passes exact boundaries, canonical SGD 0 package use, restored package session, partial refund, full reversal, non-visit sale, search, invalid input, denied and cross-tenant cases through rollback. The actual production `clientsPage` renderer passes exact 30/60/90, package, empty/retry, read-only/denied and 390px internal-scroll states. Sol independently accepted the corrected semantics and measured approximately 4.35 ms unfiltered/2.48 ms filtered at synthetic 1,000-customer/5,000-sale scale. Evidence: `V129-TRIAL-TEST-UX.md`. Target proof remains. |
| `REWARD-PROJECTION-001` | Forward-only reproduction 2026-07-30: the newly created `SPA-GLOW` customer has 1,280 points and the current business-action contract returns an available classic 1,000-point reward, but the customer portal hides the entire Rewards section because `customer_portal_capabilities` requires a versioned catalogue reward even for a valid classic programme. Sol follow-up found that the v116 positive fix still reads raw `businesses.enabled_modules`, so a v94 firm-level Loyalty disable can continue to advertise and prepare a reward that staff cannot lawfully confirm. | `VERIFIED_DATABASE` | v117 local database proof disables capability, action and preparation with zero economic mutation when effective firm Loyalty is disabled, then restores one classic reward and the canonical prepare -> exact-branch confirm -> refreshed customer projection when enabled. The current browser chain starts at 1,080 points with **Redeem 1000 points** and **Redeem now**, keeps 1,080 while pending, records the front-desk server receipt, and reloads at 80 with **More points needed** and a newest `-1000` History event at desktop and 390px. Evidence: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol acceptance and authorized production proof remain before `CLOSED`. |
| `REDEEM-SCANNER-001` | Forward-only reproduction 2026-07-30: after a current synthetic customer purchase, both Quick Earn receipt actions labelled **Redeem customer offer** open the scanner without the selected Orchard branch ID. A classic reward confirmation therefore omits `p_branch`, PostgREST reports a signature mismatch, and the UI misleadingly says the service needs an update even though `merchant_scan_redemption_qr_v93` exists. Sol follow-up then reproduced a second server defect: v93 proves branch visibility but checks Loyalty only at firm scope, allowing a direct RPC to bypass a branch-level Loyalty disable. Historical test logins are excluded. | `VERIFIED_DATABASE` | v117 requires an exact selected branch and branch-effective Loyalty write, explicitly denies authenticated/anonymous execution of obsolete v89/v93 scanners, proves a branch-disabled direct call leaves intent and all economic counts unchanged, and proves one enabled Orchard completion with replay safety. A current front-desk password session scanned the fresh customer QR in Quick Earn and rendered the server receipt; the customer then refreshed to the persisted lower balance and History event at desktop and 390px. Evidence: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol acceptance and authorized production proof remain before `CLOSED`. |
| `BOOKING-001` | Customer booking is visible only when enabled; staff appointments are searchable, editable, assignable by staff name, and distinguish duplicate customers by phone. | `IMPLEMENTED_UNVERIFIED` | Owner toggle -> customer visibility; staff amend -> customer update; branch/role denial. |
| `APPT-WHATSAPP-001` | Nestly Trial & Test 2026-07-30, reported 2026-08-01: provide a useful appointment-wise WhatsApp path. | `VERIFIED_BROWSER` | V129 exposes **Message on WhatsApp** only for a valid Singapore mobile and prefills customer, business, service, Singapore time, branch, staff and status without notes. The actual production appointment-detail renderer passes at desktop/390px with the inspected `wa.me` URL and honest manual-send disclaimer; invalid/missing phone is covered by regression. Automatic delivery remains blocked until provider, sender, templates, consent and receipts are supplied. Evidence: `V129-TRIAL-TEST-UX.md`. |
| `APPT-BLOCK-001` | Audit follow-up 2026-07-31: owner/front desk must be able to add, see, and remove a one-off staff blocked time directly inside Appointments, with no new module or navigation item. The block must survive refresh and prevent appointment suggestions, booking, and rescheduling for the full service-plus-buffer interval. | `VERIFIED_LOCAL` | Reproduction: V118 exposed working hours, leave, and branch breaks but `app/index.html` had no blocked-time writer/loader and the canonical database had no durable staff block entity. V120 now has replay-safe create/delete RPCs, staff-wide serialization, service-buffer-aware server guards, redacted cross-branch blocks, refresh loading, and report-capacity projection. The rollback-only SQL suite passes on disposable PostgreSQL 17. A real two-session harness, restricted to a separately named `v120_concurrency` database that the caller must drop whole, proves one block winner across branches plus both block-first/appointment-first writer orderings without weakening immutable evidence guards. Deterministic desktop/390px evidence includes a corrected read-only state with no write controls. Sol and Luna independently accepted the exact local candidate; `SOL-V118-V121-INDEPENDENT-REVIEW-2026-07-31.md` records the verdict. Before close: run authenticated target role/branch/refresh journeys and complete authorised release/production proof. |
| `OPSUX-001` | Recovered “Audit feature sync and usability” requirement, 2026-07-31: the workspace home must lead owner/front desk users to permission-aware daily jobs—Record sale, Add appointment, Find customer, and Set up rewards cards—without duplicating configuration or letting analytics dominate. | `SUPERSEDED` | Superseded by the newer annotated owner decision `DASHBOARD-UX-001`: those four jobs now live in the persistent app bar/navigation and must not be repeated as dashboard cards. |
| `DASHBOARD-UX-001` | Owner annotated iPad screenshots 1–7 on 2026-08-02: rename Home to Dashboard; move the branch choice into the app bar; remove the four duplicate daily-action cards; keep Performance expanded with no minimise control; visually separate the app bar and analytical body; add a Performance icon and Today range; make every KPI open useful details; explain Unique customers; add 30-day inactive customers; add icons to charts; colour busiest days green/amber/red; show revenue in SGD; show age and recorded-gender demographics; and add a services-versus-goods graph. | `VERIFIED_BROWSER` | V141 browser evidence remains the historical proof for its released layout. The newer launch-freeze decision `LAUNCH-FREEZE-001` supersedes two V141 display semantics: visiting-customer figures now require an unreversed valid visit, and the services-versus-goods chart is hidden because current item rows do not reconcile every recorded sale to a trustworthy whole. V145 must not preserve either earlier claim merely to retain a chart. V141 production deployment `dpl_87Rg8gyADszJTZFkmgzErH3zn9oA` remains historical evidence; authenticated target proof and the V145 release boundary remain open. |
| `CUSTOMER-DIRECTORY-UX-001` | Owner annotated screenshot 8 on 2026-08-02: the Customers directory must make last-visit filtering easy to choose and add a Date joined column while keeping name/phone distinction, points, credit, consent and pagination. | `VERIFIED_BROWSER` | V141 renders the named All/30/60/90 last-visit filter and Date joined while preserving phone distinction, points, credit, consent and bounded pagination. Source-bound browser acceptance proves filter persistence, never-visited/date values, an injected join-date read interruption with usable rows and retry, iPad/390px containment and no body overflow. Existing readers only; no migration. Sol accepted the exact current candidate with 0 P0/0 P1/0 P2. Evidence: `V141-DASHBOARD-CUSTOMER-USABILITY.md`. Target proof remains before `CLOSED`. |
| `CUSTOMER-GUIDANCE-UX-001` | Owner annotated screenshots 9–10 on 2026-08-02: customer profile recommendation should be clearly labelled **Peekaa's suggestion**, explain the practical next action, use a distinct surface, show the amount/date of the next expiring points batch in the Points KPI, and give Rewards an icon and distinct heading treatment. | `VERIFIED_BROWSER` | V141 source-bound browser acceptance renders one distinct **Peekaa's suggestion** reason/action, shows the synthetic 300-point batch and 28 Sep 2026 expiry in the Points KPI, and presents an icon-led Rewards heading/body at desktop/iPad/390px with no overflow. It covers no-expiry/no-programme/read-only states and an expiry-only read interruption that shows Retry without falsely saying no expiry. No automatic message, redemption or value mutation was added. Sol accepted the exact current candidate with 0 P0/0 P1/0 P2. Evidence: `V141-DASHBOARD-CUSTOMER-USABILITY.md`. Target proof remains before `CLOSED`. |
| `OPSUX-002` | Recovered audit requirement, 2026-07-31: Appointments must open as a simple Day-by-team calendar with named staff, visible booking facts, current-time context, and Week/List as secondary views. | `VERIFIED_LOCAL` | V118/V120 local regressions and deterministic desktop/390px fixtures cover named columns, observable booking details, empty columns, mobile horizontal scrolling, and the current-time rule. Authenticated target-browser evidence remains open. |
| `OPSUX-003` | Recovered audit requirement, 2026-07-31: appointment creation must remain hidden until requested, then prefill the exact selected staff/date/time only from authoritative availability; breaks, leave, missing hours, and one-off blocks must not appear free. | `VERIFIED_LOCAL` | V118/V120 local regressions cover progressive disclosure, 15-minute prefill, break/leave/missing-hours denial, and persisted one-off block consumption. Authenticated branch-switch and persistence acceptance remains open. |
| `OPSUX-004` | Recovered audit requirement, 2026-07-31: “Complete & checkout” must produce one sale/loyalty result that the customer reads consistently after refresh, without the appointment UI promising points before ledger confirmation. Disposable PostgreSQL 17 reproduction on 2026-07-31 proved the enabled path creates one SGD 60 appointment-linked sale, one 600-point earn and matching customer projections, but a firm-level effective Loyalty disable still let the V47 completion sale mint 600 hidden points. The same call replayed without a duplicate and a wrong-branch completion was denied without mutation. Root cause: `app.on_sale_recorded()` resolved the published loyalty configuration but not the sale branch's effective v94 Loyalty mode. | `VERIFIED_LOCAL` | V121 requires exact-branch effective Loyalty `rw` before a new points/stamps earn without suppressing checkout or changing existing balances. The rollback-only `SPA-GLOW` PostgreSQL 17 acceptance proves one enabled sale/+600 earn and matching `customer_get_wallet`, `customer_get_business_summary`, and `customer_get_loyalty_details`; same-call replay; firm-disabled, branch-disabled and branch-`r` sale-without-earn; prior-balance preservation; and Farah's wrong-branch zero-mutation denial. Sol and Luna independently accepted the exact local candidate; evidence is in `V121-APPOINTMENT-COMPLETION-LOYALTY-BOUNDARY-ACCEPTANCE.md` and `SOL-V118-V121-INDEPENDENT-REVIEW-2026-07-31.md`. Authenticated target owner → staff → customer refresh, reversal coverage, and authorised production proof remain before close. |
| `OPSUX-005` | Recovered audit requirement, 2026-07-31: Day view must offer clear 44px start-time choices only when the selected service plus buffers fully fits authoritative schedules, and adjacent short bookings must remain visually distinct. | `VERIFIED_LOCAL` | V119/V120 regressions cover service buffers, breaks, leave, missing-hours fail-closed behavior, adjacent bookings, exact prefill, refresh projection, and 390px layout. Authenticated target-browser proof remains open. |
| `OPSUX-006` | Recovered audit requirement, 2026-07-31: role, staff assignment, module permission, or template changes in another session must be visible on the next route/branch load instead of retaining a stale persona. | `VERIFIED_LOCAL` | V119 local regression proves each route/branch load fetches a fresh effective module projection and models a second-session downgrade. Authenticated multi-session role/template acceptance remains open. |
| `REPORT-CORE-001` | Recovered audit requirement, 2026-07-31: Reports must start from ordinary SME questions—money, utilisation, returning customers, and team—while detailed accounting remains secondary. | `VERIFIED_LOCAL` | V118 local regression proves the four decision entries and collapsed detailed report. Authenticated empty/error/mobile/export evidence remains open. |
| `REPORT-CORE-002` | Recovered audit requirement, 2026-07-31: utilisation must compare booked time with real scheduled capacity inside branch/staff hours, subtracting the union of breaks and one-off blocks and removing leave without double subtraction or false 100% caps. | `VERIFIED_LOCAL` | V119/V120 calculations cover active staff, merged break/block intervals, full-day leave, missing-hours fail-closed behavior, and truthful values above 100%. Authenticated report-to-database comparison remains open. |
| `TEAM-001` | Recovered audit requirement, 2026-07-31: scheduling must distinguish appointments from authoritative branch hours, staff hours, leave, breaks, and one-off staff blocks, including a staff member’s conflicts at another branch. | `IMPLEMENTED_UNVERIFIED` | V118/V120 implement the persisted sources, staff-wide blocks, cross-branch redaction, and missing-hours denial. Overnight schedule acceptance plus authenticated stale/branch/database evidence remains incomplete. |
| `REDEEM-001` | Customer redemption is pending until a business scan/confirm; Quick Earn must expose scanning and prevent double redemption. | `IMPLEMENTED_UNVERIFIED` | QR lifecycle, expiry, concurrency/idempotency, insufficient points, customer/business sync. |
| `ONBOARD-001` | Every firm requires Super Admin approval before an owner can create/activate the account. Website signups appear in the onboarding board. | `SUPERSEDED` | Superseded on 2026-08-01 by `SELF-SERVE-ONBOARD-001` for the public owner signup path. Consultant-led assisted onboarding may remain separate. |
| `SELF-SERVE-ONBOARD-001` | Owner message 2026-08-01: “business owner should be able to create an account and instead of request admin - is to select options and make payment through stripe and able to use.” The public business signup must therefore remove the admin-approval request and provide account creation, business/sector setup, annual-or-monthly plan and customer-capacity selection, exact no-GST price review, Stripe Checkout, and access after provider-confirmed payment. | `IMPLEMENTED_UNVERIFIED` | Reproduced approval-first predecessor; V130 now implements Auth-first self-service, locked one-workspace staging, server-derived sectors/prices, pre-expiry Checkout recovery, exact catalogue/cadence/capacity/SGD/no-GST/zero-balance paid-invoice activation, exact legal evidence and payment-pending controls. Manual Firm 360 approval/rejection is server-blocked and hidden for payment-managed firms. V132 closes the provider-trigger draft-seed conflict found by the non-production rehearsal; V129/V130/V131 SQL suites pass with zero residue and the full gate passes 1,381/1,381 plus build. Live Stripe test-mode/provider and production evidence remain required. |
| `ASSISTED-ONBOARD-APPROVAL-001` | Screenshot 2026-08-05: approving an assisted owner application is too troublesome. If Super Admin approves, that should approve and activate. Do not ask the admin to send/copy an invitation email, and do not require an approval justification note. | `IMPLEMENTED_UNVERIFIED` | For `PLAT-PENDING` with a confirmed matching owner account, Super Admin approval creates the workspace, owner staff row, default branch, subscription shell, sector assignment, consumed application state, receipt, and audit evidence immediately. Approval retry returns the existing activation without duplicate business/staff/branch/subscription rows. Missing confirmed owner account shows a precise error instead of generic failure. Rejection still requires a note. Desktop and 390px approval modal states remain readable with no horizontal overflow. |
| `SELF-SERVE-CONSENT-002` | Owner screenshots 2026-08-03: business owner signup reaches an unavailable-subscriptions page; owners must select a published plan, pay in Stripe and activate automatically after confirmed payment, without management approval. Signup must use a single plain-language checkbox whose separately highlighted and underlined **Terms of Service** and **Privacy Policy** links are optional to open, and each legal page must offer an obvious return to account creation. Owner additionally replaced the 30-day launch promise with a prospective non-refundable policy for new purchases. | `VERIFIED_DATABASE` | Root cause remains external to the form: production has no complete active annual/monthly Stripe catalogue, so the reviewed fail-closed reader returns no sellable plan; it is not a manual-approval rejection. V144 now gives that state an actionable retry/support explanation, uses “I agree to the Terms of Service and acknowledge the Privacy Policy”, opens visibly underlined legal links independently, and provides a 44px `/business?signup=1` return. A 175-migration disposable PostgreSQL replay plus `v130_self_serve_business_onboarding.sql` and `v144_self_serve_subscription_consent.sql` prove one signed first-paid activation, no new-policy refund window, immutable prior-policy windows, and zero rollback residue. Real browser evidence passes at 1440px/390px. V144 is exact in production and the V145 web artifact is live, but post-deploy inspection finds seven published sectors and zero complete annual or monthly plans. `/business` therefore still renders **Subscription setup needs configuration** and safely creates no workspace or charge. Publishing the live Stripe catalogue and verifying the signed webhook/payment path remain before close. Evidence: `V144-SELF-SERVE-SUBSCRIPTION-CONSENT.md`, `V145-LAUNCH-FREEZE-AUDIT.md`. |
| `BUSINESS-ONBOARDING-IDENTITY-001` | Owner screenshot and report 2026-08-03: after business login/signup the unavailable-subscription page is vague, does not identify the signed-in email, and offers no sign-out action. Every authenticated business setup state must state the current email and provide an explicit sign-out; signing out must clear only the local browser session and return to business sign-in. | `REPRODUCED` | Production build `addb6f0` renders the empty-catalogue state without identity or sign-out, while the adjacent load-error/payment-pending states do expose sign-out. Static reproduction is in `app/index.html`; the owner screenshot shows the exact production state. Required closure: existing email/password and Google sessions survive refresh, identity is escaped and visible, local sign-out returns to sign-in, and empty/error/retry/mobile paths remain truthful. |
| `AUTH-BUSINESS-GOOGLE-001` | Owner message and Google Auth Platform screenshot 2026-08-02: business login and signup must offer Google login using the already-created Peekaa Web OAuth client. | `VERIFIED_BROWSER` | Production Supabase Auth reports Google enabled, but served commit `d993624` has no Google control and V134 source has no `signInWithOAuth` or business OAuth callback. V135 now presents one 44px Google action on business sign-in and owner signup, requires explicit legal acceptance at both provider entry points because Google may create an unknown identity, requires that acceptance in a recent same-tab single-use marker, scrubs callback credentials immediately, canonicalizes origin before start, preserves server-derived authorization, and excludes platform-admin, customer and native-companion entry. New workspace creation remains durably server-gated by `p_legal_accepted=true`. Focused and complete counts are indexed in `V135-BUSINESS-GOOGLE-AUTH.md`; desktop and 390px Chrome/IAB acceptance proves executable Peekaa entry/signup, no horizontal overflow, consent recovery, and no admin Google action. Sol independently accepted the frozen candidate with no P0/P1/P2. Production Supabase still has `https://peekaa.asia` as Site URL and lacks `https://www.peekaa.asia{,/**}` redirect rows, so exact config correction, subsequent owner release approval, first-time/existing synthetic provider acceptance and production smoke remain. |
| `AUTH-CONSENT-001` | Owner screenshot and clarification 2026-08-02: the Terms/Privacy checkbox belongs to account creation only. Normal email/password or Google sign-in must not show or require it; a Google identity with no existing business persona must still enter the consent-gated signup path before workspace creation. | `VERIFIED_BROWSER` | V138 removes the checkbox from ordinary email/Google sign-in. Existing active business staff pass an authenticated Google-only server admission check. Google signup first records a 30-minute single-use no-PII consent attempt pinned to the exact active 2 August Terms/Privacy versions and SHA-256 values; the callback consumes it into private append-only user-bound evidence. The provider session stays in a non-persistent client until admission succeeds, so close/crash/reload or another tab cannot inherit an unadmitted identity. New-identity login, unchecked/wrong-version/expired/forged/cross-user/non-Google callbacks fail closed in rollback SQL; actual-renderer Chrome verifies unchecked/checked signup, checkbox-free login, accepted/rejected/forged/expired/replayed callbacks and interruption immediately after token exchange at 390px. Sol independently accepted the corrected frozen candidate with 0 P0/P1/P2. Production provider identity proof remains required. |
| `AUTH-SESSION-001` | Owner clarification 2026-08-02: keep customer and business users signed in on their own device until they explicitly sign out, subject only to provider revocation/security termination. Reload, tab close/reopen, PWA relaunch and ordinary later-day return must restore and refresh the same session. | `VERIFIED_LOCAL` | V138 proves the browser client uses `persistSession:true` and `autoRefreshToken:true`, ordinary sign-in/routing contains no implicit sign-out, and PWA updates do not cache authenticated responses. A real later-day/revocation production profile remains required before close. |
| `MODULE-001` | Super Admin controls sector templates and firm/branch overrides; owners cannot escape the effective set; inventory and Customer Intelligence are hidden from firms. | `IMPLEMENTED_UNVERIFIED` | Sector/firm/branch matrices for owner/manager/front desk and direct-route denial. |
| `MODULE-PROJECTION-001` | Forward-only reproduction 2026-07-30: a newly approved `SPA-GLOW` firm has firm-level `giftcards=disabled` and the global Inventory prohibition in the current database, but the newly created owner still sees Gift cards in workspace navigation because the client-facing module projection reads the pre-override sector array. Sol follow-up found the branch matrix incomplete: firm-only entry checks can hide a branch-only enabled route, branchless gift-card issuance can bypass a branch disable, and the claimed existing-liability acceptance had no fixture. | `VERIFIED_DATABASE` | v115-v117 local database tests prove firm and branch `disabled/r/rw/inherit` projection and writer boundaries, including no projection for unassigned front desk, one branch-only enable that does not broaden its comparison branch, zero-mutation disabled gift issuance, and an existing SGD 50 liability transferred once to SGD 10 customer credit with SGD 40 remaining. The same gift-redeem key replays without a second liability reduction, credit row or immutable receipt. Current staff browser proof covers the exact Orchard scanner; disabled-module absence remains regression/database evidence. Evidence: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol acceptance and authorized production proof remain before `CLOSED`. |
| `PLATFORM-001` | Super Admin/Admin/Sales permissions, searchable firms, streamlined kanban, inactivity notices, contact actions, branch/customer drill-down, reports, billing, and commissions. | `IMPLEMENTED_UNVERIFIED` | Role-isolated browser and DB scenarios, report scope, billing webhooks, calculation fixtures. |
| `PLATFORM-SELF-SERVE-PARITY-001` | Owner screenshots 2026-08-03: the platform Onboarding page still says **Business applications awaiting approval** and that no owner/workspace can be created before Super Admin approval, contradicting the current paid self-service flow. Assisted applications may remain, but must be explicitly separated from website signups and must never imply that a self-service Stripe purchase awaits management approval. | `VERIFIED_BROWSER` | V148 labels the legacy queue **Assisted applications**, states that website signups activate only after Stripe confirmation, and leaves approve/reject on assisted rows only. `tests/platform/v148-onboarding-admin-clarity.test.mjs` locks the distinction; the production-component browser fixture renders the corrected copy at 1440px and 390px. Production verification remains release-gated. |
| `PLATFORM-SELF-SERVE-VISIBILITY-001` | Screenshot 2026-08-04: a new public self-service business signed up, but the Super Admin Onboarding page top card shows **0 pending** assisted applications and no visible business name, making it look impossible to find, approve, or reject the new firm. Follow-up screenshot says signups are pending Stripe, but there is no Stripe payment path and no admin-visible lead to approve for bank transfer/cash. Production check on 2026-08-04 found recent Auth accounts with no staff, self-serve onboarding, or assisted application row, so account-only signups were invisible to Super Admin. Current product truth says self-service signups are Stripe-payment-managed and must not expose manual approve/reject, but Super Admin must still see the account/business state immediately in Onboarding; when the Stripe plan catalogue is unavailable while sectors are published, a confirmed owner must be able to submit an assisted manual-help application visible to Super Admin without activating access. | `IMPLEMENTED_UNVERIFIED` | Admin Onboarding shows separate sections for **Account signups needing business details**, **Website signups and paid workspaces**, and assisted applications. Auth-only rows show email, confirmation/setup status and contact action; payment-managed rows show business name/contact/subscription/onboarding state and Firm/Billing links; approve/reject controls remain absent for account-only and payment-managed paths; assisted applications still retain approve/reject. If Stripe plans are unavailable and at least one business sector is published, signed-in confirmed owners see a manual-help/bank-transfer application form that writes one assisted application and one first-write audit record only; it does not create a business, mark payment, or unlock access. Required proof: `tests/platform/v148-onboarding-admin-clarity.test.mjs`, `tests/business-ui/v130-self-serve-business-onboarding.test.mjs`, rollback-only DB acceptance for `request_self_serve_manual_application_v159`, rollback-only DB acceptance for `platform_list_account_signups_v160`, browser desktop/390px, production `/api/build`, and production read-only confirmation that applied RPCs exist and admin queue renders. |
| `BUSINESS-SIGNUP-DEMO-163` | Owner screenshots and messages 2026-08-04: business signup must be manageable as inbound leads. Google signup must start, then the owner must choose monthly/yearly Stripe payment or **Request demo**; demo/manual-payment requests collect company name, owner name, phone and optional email/account email, tell the applicant Peekaa will follow up within 48 hours, and appear in Super Admin Onboarding where assisted applications can be approved or rejected. Owner follow-up: account-only signups must be manageable, not only visible; Stripe should collect the existing SGD 148 monthly / SGD 1,188 annual subscription amounts. Owner screenshot 2026-08-05: Google signup on `/business#/` still shows the older pre-V163 signup copy and fails before redirect, indicating a stale installed/browser shell can keep sending old legal-version evidence. | `CAPTURED` | V163 updates the business OAuth Terms digest to the active 2026-08-04 manifest, explains signup → pricing/demo, keeps Stripe payment as the only automatic access path, and exposes **Request demo / manual help** from the setup page even when Stripe catalogue exists. V164 adds durable Super Admin triage for account-only business signups: mark contacted, mark follow-up, archive, searchable note evidence and audit log. This remains CRM-only and cannot create a business, staff row, invoice, subscription or entitlement. Stripe checkout continues to use the reviewed server-side V124/V162 catalogue: SGD 148/month, SGD 1,188/year, +1,000 customers at SGD 10/month or SGD 120/year, with verified Stripe invoice/webhook activation only. V165 bumps the PWA cache version and makes stale-shell replacement notify open tabs to reload once, so auth/legal signup code converges without clearing user sessions or caching private responses. Required proof: business Google consent test uses 2026-08-04 Terms hash; business setup test shows monthly/yearly checkout plus demo/manual-help path; platform queue test shows triage actions and assisted approve/reject separation; V164 DB test proves private Super Admin-only triage and no access/subscription side effects; PWA regression proves stale cache deletion/reload notification; production browser smoke after release. |
| `BUSINESS-SIGNUP-ENTRY-167` | Owner clarification 2026-08-05: before asking for business name, full name or other setup details, the public business entry must ask whether the user wants to **Request a demo** or **Set up business**. Demo means consultants contact the lead and there is no account to log in. Set up business means the user proceeds to owner account/business setup, then chooses Stripe payment for automatic activation after verified payment or manual payment for Super Admin approve/reject. | `VERIFIED_LOCAL` | V167 changes `/business?signup=1` into an intent choice with **Request a demo**, **Set up business**, and **Join an existing business**. Demo opens a no-account consultant-contact email flow and does not call Supabase Auth, Stripe checkout, the V159 manual-payment application RPC, or invite acceptance. Set up business retains the owner-account path and explicitly separates verified Stripe auto-activation from authenticated manual-payment Super Admin approval. Manual payment remains a Super Admin approve/reject workflow after payment evidence is checked; staff invite remains separate and does not require Stripe. Local proof 2026-08-05: `node --test tests/business-ui/v167-entry-choice-demo-before-account.test.mjs tests/business-ui/v130-self-serve-business-onboarding.test.mjs tests/business-ui/v151-mobile-invite-stripe.test.mjs tests/business-ui/v135-business-google-auth.test.mjs tests/business-ui/v144-self-serve-consent.test.mjs` passed 34/34; `npm run build` passed. Post-release browser smoke remains required if deployed. |
| `PLATFORM-ADMIN-APPROVAL-CACHE-167` | Owner screenshot 2026-08-05: Super Admin Onboarding still shows the old **Email setup/demo link**, **Mark contacted**, **Follow up** and **Archive** controls instead of the requested direct **Approve**, **Reject** and **Pending** decisions. Root cause: the corrected platform console code can be live while `index.html` still points at the old fixed `/platform-console.js?v=20260802-v134` asset URL, allowing a browser/PWA cache to continue loading the stale admin UI. | `VERIFIED_LOCAL` | V167 bumps the platform-console script reference to `/platform-console.js?v=20260805-v167-admin-approval` and adds a regression that fails if the shell points back to the old V134 cache key. The live admin console must load the same asset that contains **Use Approve, Reject or Pending**, `data-account-signup-triage`, and no **Email setup/demo link** or **Approve/reject becomes available...** copy. Required production proof: `/api/build` matches the deployed commit, root HTML contains the V167 asset key, and the deployed V167 platform-console asset contains only the direct admin-decision copy. |
| `PLATFORM-APPROVAL-ADMISSION-168` | Owner screenshots 2026-08-05: Super Admin can press **Approve** and the row shows **Admin decision: Approved**, but the same approved business signup still cannot sign in and sees **No business account was found**. Root cause: V164/V167 approval only wrote platform triage; `complete_business_google_oauth_v138` still admitted only active staff/workspace users. | `IMPLEMENTED_UNVERIFIED` | V168 updates Google business admission so a Super Admin-approved account-only business signup may sign in and continue setup with `requires_business_setup=true` and `platform_account_signup_approved`. This does not create workspace, staff row, subscription, invoice, entitlement, or paid status; workspace access still requires business details plus verified Stripe payment or manual billing. Pending/rejected signups remain blocked. Required proof: V168 migration applied in production, Google sign-in smoke for an approved account reaches setup instead of stale rejection, rejected/pending stay blocked, `/api/build` matches deployed commit. |
| `CUSTOMER-SIGNUP-PROFILE-CONSENT-163` | Owner screenshot and message 2026-08-04: customer signup currently captures only phone. Signup must capture date of birth and gender with only **Male** and **Female** options, and Terms/Privacy plus marketing consent must be one statement with one checkbox instead of two boxes. | `CAPTURED` | V163 keeps verified-phone registration, persists DOB plus gender in the private customer profile through a server RPC, rejects any gender other than male/female, and collapses the initial create-account legal/privacy/marketing statement into one checkbox. Required proof: customer signup regression checks one checkbox at OTP start, no second marketing checkbox there, profile has DOB/gender, RPC payload includes `p_gender`, SQL migration adds private constrained gender and extends registration idempotency hash. |
| `PLATFORM-LAYOUT-OVERLAP-001` | Owner screenshot 2026-08-03: explanatory text in the Super Admin Firms filter overlaps the next row's label and control. Labels, helper text, inputs and actions must occupy separate readable layout space at desktop and 390px, including translated long labels, with no horizontal overflow. | `VERIFIED_BROWSER` | V148 removes the negative helper margin and gives search/helper content its own grid space. The browser fixture verifies desktop and 390px with no outer overflow; the 390px header was also corrected to shrink its locale control instead of exceeding the viewport. Exact CSS contracts are in `tests/platform/v148-onboarding-admin-clarity.test.mjs`. Production verification remains release-gated. |
| `PLATFORM-PROSPECT-001` | Owner screenshots and report 2026-08-03: the platform CRM does not visibly offer a selectable Kanban/List view and the owner is unable to add prospects. The existing assisted-sales workflow must expose both views, preserve filters/view in the URL, and persist one complete prospect under retry-safe identity. | `VERIFIED_BROWSER` | V148 exposes Kanban/List controls, persists `view=list` in the URL and reuses one idempotency key through lost-response retry. Browser acceptance proves Kanban and List are mutually exclusive at 390px, each fits without overflow, and **New prospect** opens a labelled modal whose controls are all at least 44px. Persistence/role boundaries remain covered by the existing V76 suites; production verification remains release-gated. |
| `PLATFORM-PNL-001` | Owner screenshot and report 2026-08-03: Super Admin cannot find Peekaa's company P&L and operating expenses. The existing platform-finance scope must distinguish Peekaa subscription money from merchant sales, project provider-confirmed cash/refunds, record append-only operating expenses with reversals, and reconcile an exact period net result. | `VERIFIED_DATABASE` | V146 is forward-integrated onto the V145 baseline with a Super Admin-only **Cash P&L** route, provider-confirmed cash/refund projection and immutable operating-expense reversals. A fresh disposable PostgreSQL replay through all 179 migrations, V146 database acceptance and rollback pass; browser acceptance renders the empty/zero truth and 44px actions at 1440px/390px without overflow. Independent review and production verification remain gated. |
| `PLATFORM-ACCOUNTING-001` | Owner report 2026-08-03: accounting modules for P&L, expenses and books are not visible in the platform console. Existing accepted scope must surface automated double-entry books, trial balance, P&L, balance sheet, general ledger and controlled corrections without deleting posted history or claiming statutory automation that is not implemented. | `VERIFIED_DATABASE` | V147 is forward-integrated with effective-dated identity, balanced journals, trial balance, P&L, balance sheet, general ledger, immutable documents/corrections and period locks. Fresh 179-migration replay, V147 backfill, rollback and two-session serialization (`1:20000`) pass with zero fixture residue. Browser acceptance covers empty accounting truth at 1440px/390px. Independent review and production verification remain gated. |
| `SUBSCRIPTION-OPS-CRM-156` | Owner request 2026-08-04 and supplied bank screenshot: build Peekaa's internal prospect-to-payment CRM, canonical quotations/invoices/verified-payment receipts, private PDFs, billing contacts, auditable document email, subscription dates/renewals/payment-failure queues and Stripe-driven lifecycle automation without changing merchant or financial authorities. | `VERIFIED_DATABASE` | V156 extends V76/V86 CRM and V77 paid truth with one Stripe invoice/document mapping, immutable private PDF revisions, two-person manual-payment verification, subscription operations and idempotent delivery/task automation. The legal UEN `202634502E` comes from existing owner-confirmed product truth; registered address, billing email, GST decision, default terms and Resend/dispatch secrets remain intentionally unset, so finalisation/delivery fails closed. Production migrations `20260804051519_nestly_v156_subscription_operations_crm` and `20260804063238_nestly_v156a_v151_invite_search_path_hardening` are applied; V156 rollback-only SQL acceptance passes; V156a production inspection confirms `pg_catalog, public` and unchanged execute ACLs; dispatcher v1 and Stripe webhook v4 are ACTIVE; unauthenticated probes return 403/400; email remains fail-closed as `secret_unconfigured`. The integration started from deployed V157 build `65887b0f...`; affected application regressions pass 207/207, migration/security/V156 tests pass 37/37, and quality/runtime/manifest/canonical/build/diff gates pass. Sol independently accepted with P0/P1 none. Vercel deployment `dpl_9n5oj9vUMtodbcbsvyfQgGs49pSc` is Ready; production `/api/build` matched `0ba87f8b...`, `/admin` returned 200 with security headers, and the deployed platform bundle matched source. The repository-wide suite was sampled once and remains red only across unrelated historical/legacy expectations predating V157. Authenticated production viewports and live-provider payment/email remain unverified because admin credentials, owner billing configuration, email secrets and authorised Stripe fixtures are unavailable. |
| `ADMINBOOT-001` | Screenshot 2026-07-29: opening `nestly.asia/admin` shows a blank page. The direct route must immediately explain whether it is loading, signed out, updating, offline, denied, or ready—never leave the operator guessing. | `VERIFIED_BROWSER` | Root cause fixed: frozen customer-helper Proxy invariant plus an un-awaited platform renderer. The direct route paints a loading state before session work, awaits the renderer, catches rejection, and fails visibly when the platform module or route contract is unavailable. Executable harnesses cover frozen helpers, async rejection, missing module, overview query and nested query routes; production-component browser evidence covers ready/denied states and desktop/mobile overflow. Production smoke remains release-gated. |
| `ADMINIA-001` | Screenshot 2026-07-29: the platform backend has too many equal-weight modules and buttons. Reorganize it for speed, remove or demote unnecessary controls, and preserve all required roles/data behind straightforward task flows. | `VERIFIED_BROWSER` | Five primary jobs replace nine equal-weight links; Today covers applications, onboarding, billing, commissions and reconciliation incidents with one action each; role navigation is scoped; Firms hands scope to canonical Reports; Finance/System health responsibilities are separated; common forms use names/SGD/percentages; directories are bounded; refresh state is encoded in the URL. Firm 360 has one bulk module action and the visual manager shows 6 groups/19 selectors/one Reset/one Save/zero per-module Edit buttons with no desktop/mobile overflow or visible sub-44px targets. Denied access has no dead retry, while a transient access-check failure has exactly one retry and states that access was not removed. Today now exhausts both the v88 attention cursor and the delegated Admin/Sales v105 firm cursor, including actionable work after row 50; all domains label coverage complete/partial/unavailable, partial derived KPIs are lower bounds, and a cap or RPC failure cannot produce a false exact KPI or “caught up” state. The coverage increment passes **33/33** focused tests in addition to the recorded **80/80** v105 and **1,074/1,074** repository evidence. Artifacts and exact limitations are indexed in `V105-SUPER-ADMIN-UX-AUDIT.md`. Authenticated production smoke, independent reviewer acceptance, and release remain gated, so the issue is not `CLOSED`. |
| `INTEL-001` | Nestly consultants need sector/firm/branch customer intelligence and actionable consultative advice; firm owners must not access the module directly. | `IMPLEMENTED_UNVERIFIED` | Assigned consultant scope, aggregation math, recommendation provenance, owner denial. |
| `QUICK-001` | Quick Earn uses enabled catalogue items/variants, supports phone search, fast double-confirm corrections, and consistent points/sale outcomes. | `IMPLEMENTED_UNVERIFIED` | Duplicate names, catalog off/on, correction/reversal, lost response, double tap, ledger sync. |
| `RECORDSALE-001` | Nestly Trial & Test 2026-07-30, reported 2026-08-01: rename the staff-facing “Quick Earn” workflow to the immediately understandable “Record sale”. | `VERIFIED_BROWSER` | V129 relabels the staff-facing navigation/global/mobile/profile/checkout surfaces **Record sale**, preserves `#/till`, permissions and sale writers, and curates Chinese/Malay primary labels. Existing checkout suites plus the actual production `tillPage` denied-role renderer pass at desktop/390px. Evidence: `V129-TRIAL-TEST-UX.md`. Target proof remains. |
| `QUICK-002` | Owner message 2026-07-31: remove Gift card from Quick Earn. Quick Earn must have zero gift-card issue/apply/redeem controls while valid gift value remains safe and operable in the dedicated Gift Cards workflow. | `VERIFIED_LOCAL` | V122 source/full-suite regression proves Quick Earn contains no gift-card control or RPC while the dedicated Gift Cards workflow and reviewed v117 liability boundary remain. Authenticated target desktop/390px and production proof remain before close. |
| `CALENDAR-001` | Owner message 2026-07-31: “Schedule not working”; test two staff and make pressing the calendar create appointments from the selected staff/time like Google Calendar. | `VERIFIED_LOCAL` | Disposable-branch rollback acceptance books Alina Chen and Maya Lim through the production manual-assignment RPC and verifies both persisted staff IDs. Desktop calendar evidence shows two named columns and a slot click opening the exact staff/time form; authenticated target refresh/customer projection remains before close. |
| `GROW-001` | One guided Grow setup, visual overview, editable result, no hard refresh, understandable playbooks/studio, sector-specific recommendations with flexible ranges. | `VERIFIED_LOCAL` | Local v97-v102 UI tests exist; still requires desktop/mobile browser task completion and persisted reload evidence. |
| `GROW-002` | Owner message 2026-08-02: “relook into the grow modules - the page is too flooded with information and left right bottom are so much information.” Grow must be usable without training: one obvious start/continue action; one complete overview containing every configured reward programme and every programme still available to set up; and clicking a configured or not-yet-configured item must open that exact edit or create control instead of a generic module page. | `VERIFIED_BROWSER` | Red state: two equal desktop columns, six first-viewport controls and no overview status for bring-back/referrals/memberships/gift cards. V136 now provides one primary setup action and compact single-column rows across all seven programme families, including every stable reward UUID and Bring-back rule ID; exact existing/new/missing-ID routes through the real production editors; honest live/scheduled/paused/off/not-set-up/unavailable/not-included/read-only/dependency states; zero dead manager/absent-module/unavailable/Retention-only/stale-route writers; and collapsed advanced settings. Production-component Chrome passes owner/manager, empty/partial/all-read-failure/configured-off/future-dated/limited-module/Retention-only, exact reward and Bring-back refresh, stale-route fail-closed actions, popup retry/draft safety, 1440/390/412 and no overflow under source `0588c88…`; complete gate passes 1,410/1,410 plus build and strict 289-control inventory. Four rejected candidates were closed with regressions; Sol accepted frozen candidate `e3917c64…` with P0/P1/P2 all zero. Authenticated target persistence remains pending. Evidence: `V136-GROW-STREAMLINE.md`. |
| `GROW-EDIT-001` | Owner screenshot and clarification 2026-08-02: the second reward row (“where points can redeem what food”) and some programmes cannot be edited. Selecting any configured row must open an editable draft for that exact stable record, not a read-only editor merely because the published record is absent from a stale/current draft. | `VERIFIED_BROWSER` | V138 production-renderer Chrome accepts exact duplicate-name reward and Bring-back IDs at desktop/mobile. Disposable SQL now passes exact reward, Birthday and Bring-back materialisation; service eligibility; unrelated-row preservation; true old-hash lost-response replay; stale-hash/cross-tenant/anonymous denial; audit uniqueness; and no publication. Target/production migration and post-release proof remain required. |
| `GROW-VISIBILITY-001` | Owner clarification 2026-08-02: every applicable Grow programme needs a simple on/off control. Off/inactive programmes must not appear in the customer view, while prior customer value and immutable history remain safe. | `VERIFIED_LOCAL` | The V138 rollback suite behaviorally proves disabled Referral creates no reward and inactive Membership creates neither membership nor sale. The current C45 rollback suite proves Birthday OFF hides new eligibility after customer refresh while retaining an existing immutable promise. Earn/catalogue reward, Bring-back and Gift-card off boundaries remain pinned by their existing active filters and liability-preserving suites. Production owner→customer proof remains required. |
| `GROW-HUB-001` | Owner clarification 2026-08-02: consolidate Grow into one overview and one workspace. Earn, Birthday, Bring-back, Referrals, Memberships and Gift cards are submodules opened from the overview; finishing an edit returns to the overview instead of requiring multiple left-side Grow tabs. | `VERIFIED_BROWSER` | V138 exposes one entitlement-neutral `#/grow` nav, exact submodule rows, Back/Done return, read-only/disabled/empty/retry states and passes Chrome at 1440 plus 375/390/412px with 44px controls and no horizontal overflow. A retention-only owner can open/create the shared draft without Loyalty access. Production proof remains required. |
| `GROW-EDITOR-ISOLATION-001` | Owner live screenshot 2026-08-02, production build `c22eb2189f4b`: selecting **Earn** at `#/loyalty/<business>/earning` still renders the **Birthday benefit** form below the points editor. Selecting any Grow row must replace the overview with only that exact programme editor; points must never open Birthday, another adjacent form, or an editor appended beneath the overview. | `VERIFIED_PRODUCTION` | The red V139 regression passes 5/5, prior V138 closure passes 11/11, source-bound Chromium passes exact isolation at desktop/390px, and repository validation passes 1,434/1,434 plus build. Sol independently accepted with P0/P1/P2 all zero. Production deployment `dpl_9YfeQfJ7tkqLAyEs81iL54oE5mWz` is READY on `www.peekaa.asia`; authenticated Cubbly Earn verification found one programme editor and zero reward/Birthday/redemption sibling editors with no overflow. Production already contained the reviewed V138 migration and six expected RPCs, so no duplicate DDL was applied. Evidence: `V139-GROW-EDITOR-ISOLATION.md`. Other programme-write and owner→customer proofs remain in their own ledger rows. |
| `GROW-AUTHORITY-LABEL-001` | Final authenticated V139 production walkthrough on 2026-08-02, build `b04727b1ebba`: Cubbly's owner can edit the exact Earn draft, but its header says **Read only**. An authorized owner with an editable draft must see **Editable draft**; a manager or other read-only role must see **Read only** and zero writers; an authorized owner without a draft must see the create-draft action. | `VERIFIED_PRODUCTION` | Reproduced on V139, then corrected by V140. Red-first failed 0/4; focused V138–V140 passed 20/20; source-bound Chromium passed owner desktop and manager 390px; the repository gate passed 1,438/1,438 plus build; and Sol independently accepted with P0/P1/P2 all zero. Production deployment `dpl_8GDnyWe6UrhVbed3qF4gy7fENdMY` is Ready and canonical build `370c5bfcc350` serves the V140 helper. Authenticated Cubbly Earn verification found one visible **Editable draft**, enabled Save/Review controls and no visible Birthday sibling. No programme/customer mutation was performed. Evidence: `V140-GROW-DRAFT-AUTHORITY-LABEL.md`. |
| `GROW-AUTO-001` | Owner follow-up 2026-08-02 after the measured automatic-setup review: “Please close the gap.” The review found four taps for both a new and existing draft, a single preselected goal followed by two read-only screens, hidden continuation actions on a 375×667 phone and phone landscape, an abrupt handoff into the detailed editor, and a label that implied broader automatic configuration than the one governed loyalty draft actually created. | `VERIFIED_BROWSER` | `SPA-GLOW` red reproduction measured new/existing/retry at four/four/five taps with zero meaningful choices and off-screen actions. V137 now creates in two actions through one truthful review sheet, resumes an existing draft in one action/zero generator writes, shows a concise model/reference-price/reward-threshold handoff, reuses the same retry identity and never publishes. Scope explicitly excludes Birthday, Referrals, Memberships and Gift cards; real fulfilment cost is disclosed as absent. Source-bound Chrome covers `SPA-GLOW` **320 points**, `CAFE-HARBOUR` **8 stamps**, and the governed `classic` null threshold as **Set in the draft**, plus owner/read-only, cancel/Escape, failure/retry, concurrent draft, exact editor handoff, 1440, 375×667, 390, 412 and 844×390 with visible 44px actions/no overflow. Sol rejected two earlier candidates for currency-unit and null→0 truthfulness bugs; both have red-first/source-bound regressions. Sol accepted frozen digest `8332d446…` with P0/P1/P2 all zero. Evidence: `V137-MINIMAL-AUTO-REWARDS.md`. Authenticated target persistence remains pending. |
| `REWARD-OVERVIEW-001` | Owner messages 2026-07-31 and 2026-08-01: business owner needs one game-like overview containing birthday, whatever points/stamps programme is configured, and every reward; it must explain how each is achieved, including sequenced milestones such as five stamps for one reward and another five for the next, and clicking a specific reward must edit that exact reward. | `VERIFIED_BROWSER` | Red-first reproduction confirmed V122 omitted classic points redemption/earning, used non-interactive cards and generic Add routing. The amended production component shows earning, applicable classic/catalogue rewards, paused/archived rows and birthday in one view; stable UUID clicks open the exact reward without losing input focus, birthday opens its editor, draft-first routing prevents direct publication, incomplete reads expose Retry, and read-only manager gets zero writers. Sol rejected hash `8a39752…` after finding a closed-table birthday read and overstated paused/scheduled/ended availability. V127 replaces it with a tenant-scoped Loyalty-read RPC while retaining closed table/draft authority; its server `as_of` governs paused/future/ended availability instead of the owner's device clock. Focused 13/13, local rollback SQL owner/manager/denied/cross-tenant/anon authority, and Chromium owner/manager 1440/390 evidence pass under source hash `0182cb1…`. Sol independently accepts that exact candidate with no P0/P1/P2 findings. Exact reward database before/after, linked-customer mirror, conflict/lost-response and authorized production evidence remain before close. Evidence: `REWARD-OVERVIEW-OWNER-ACCEPTANCE.md`. |
| `REWARD-SETUP-001` | Owner message and screenshot 2026-08-01: the Grow page still has too many equal-weight controls, the one-page rewards overview is not obvious, and the owner does not know where to start. Provide one button that sets up rewards automatically through a simple popup. | `VERIFIED_BROWSER` | Screenshot and red-first tests reproduced four competing Trigger/Who/Reward/Result cards, separate edit actions and economics before a clear start. V128 now puts one dominant **Set up rewards automatically** button and the complete published Rewards overview before collapsed secondary settings. The accessible popup makes zero writes on open/cancel, uses governed sector/catalogue inputs, creates or resumes one editable draft with a stable retry key, never publishes, and truthfully leaves cost/profitability for review. Chromium passes owner/manager desktop and 390px including focus, retry, existing draft and stale-tab resume under source hash `438a10a…`; a 167-migration disposable PostgreSQL rehearsal proves facial mapping, replay, one-draft serialization, read-only/disabled/cross-tenant/anon denial and zero rollback residue. Full gate: 1,362/1,362 plus build. Sol independently accepted exact staged candidate `10fb9a1…` with no P0/P1/P2 findings. Exact target owner publication → staff fulfilment → linked customer refresh and production proof remain before close. Evidence: `V128-SIMPLE-REWARDS-SETUP.md`. |
| `PROFIT-AI-001` | Owner message 2026-07-31: product cost and profitability must make AI-assisted reward-programme setup easy; the current experience is not understandable. | `VERIFIED_LOCAL` | Disposable-branch migration proof confirms the nullable fail-closed cost column and ACLs; arithmetic/browser evidence shows SGD 68 price, SGD 24 cost, SGD 44 gross profit, 64.7% margin, and editable post-reward contribution. Authenticated denied-role and production proof remain. |
| `CUSTOMER-CATEGORY-001` | Owner message 2026-07-31: customer company view needs categories such as Personal care for facial/spa firms. | `VERIFIED_LOCAL` | Full-suite mapping regression and 390px browser evidence group three linked firms into Personal care, Food & drink and Fitness with zero horizontal overflow and no self-link writer. Authenticated empty/error/many-card target evidence remains. |
| `FEEDBACK-SYNC-001` | Owner message 2026-07-31: feedback does not sync; owner requested a Google review handoff when five stars is clicked. The selective five-star gate is not accepted because Google Maps policy prohibits selectively soliciting positive reviews; the compliant requirement preserves the source complaint while offering the same configured link after any successfully persisted genuine rating. | `VERIFIED_LOCAL` | Disposable-branch contract confirms the enabled tenant feedback trigger and authenticated-only preference projection; regression proves Realtime refresh routing and the same optional review link after every persisted 1–5 score. Authenticated two-session target evidence remains. |
| `I18N-001` | Business workspace toggles English/Chinese/Malay interface; no side-by-side Chinese content fields; customer stays English. | `IMPLEMENTED_UNVERIFIED` | Full-route translation inventory, persistence, missing-key and mixed-language check. |
| `BRAND-001` | Business logo/profile and programme images save and sync to customer cards; reported upload permission error must be fixed. | `VERIFIED_LOCAL` | v102 local regression passes; still requires authorized target storage upload, reload, owner/customer visual evidence, and denied-role test. |
| `PACKAGE-001` | Service variants, package list value/discount, owner-configurable package points, customer-owned package/voucher visibility and deduction in Quick Earn. | `VERIFIED_LOCAL` | v102 local regression/DB contract passes; still requires real-browser owner -> staff -> customer run and target DB evidence. |
| `GIFT-001` | New gift-card issuance is absent when disabled, while valid existing gifts remain redeemable. | `VERIFIED_DATABASE` | v117 local database proof denies new issuance with zero new gift rows, preserves a synthetic existing SGD 50 liability, transfers SGD 10 to exactly one `gift_card_load` customer-credit row, leaves SGD 40, and records one immutable keyed receipt. Replaying that key returns the prior result without a second economic or receipt row. Disabled Gift-card UI remains covered by regression evidence; customer gift-history production projection remains unproved. Evidence: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol acceptance and authorized production proof remain before `CLOSED`. |
| `NAV-001` | Left navigation must not auto-scroll, hard-refresh, blank the workspace, or lose draft state. | `IMPLEMENTED_UNVERIFIED` | Route sweep with saved scroll/draft state, slow network, chunk failure/retry, mobile. |
| `MOBILE-001` | Customer and business experiences must be PWA/mobile-ready now and compatible with a later native wrapper. | `IMPLEMENTED_UNVERIFIED` | Installability, safe areas, camera, passkey, push, offline/reconnect, iPhone/Android acceptance. |
| `MOBILE-STORES-001` | Owner message 2026-08-01: prepare Nestly for App Store and Google Play publication as soon as possible. The native phase is now active; a wrapped website alone is not sufficient evidence of store readiness. Sol review 2026-08-01 reproduced that packaged origins were absent from gateway CORS, Turnstile failed on `localhost`, shared/auth email links leaked the WebView origin, and bundled entrypoints lacked complete in-document CSP coverage. Owner message 2026-08-01 after Sol acceptance: “ensure that all task are completed and reviewed - if not please close the gap and push and deploy”; this is recorded as V125 release approval, not proof of store publication. | `IMPLEMENTED_UNVERIFIED` | V125 is merged and deployed. Production functions accept the exact packaged origin and reject a hostile origin; production HTML/bridge smoke proves CSP on both entrypoints and canonical HTTPS links. Android targets API 36; deep links, permission copy, bounded adapters and branded icons remain. Production Turnstile must still add the exact `localhost` hostname, and signed archives/physical-device acceptance remain blocked on full toolchains, signing teams and devices. Store metadata/screenshots/review remain. Sol accepted the exact candidate. Evidence: `V125-NO-GST-NATIVE-READINESS.md`. |
| `MOBILE-STORES-002` | Owner message 2026-08-01 after the store audit: “Please ensure the app publish is ready to live.” The audit reproduced four additional publication gaps on the V129 candidate: packaged native code exposes Stripe subscription purchase/portal controls; authenticated account creation has no in-app deletion request; both live association files return HTTP 404; and the iOS 1024px icon contains an alpha channel. This Mac also has no full Xcode, Java/Android SDK or signing identity and less than 1 GiB free. Owner follow-up 2026-08-01: “Please close the gap and report back to me the status.” | `IMPLEMENTED_UNVERIFIED` | V131 keeps website Stripe signup unchanged while packaged builds are purchase-free companions, adds one authenticated/replay-safe account-deletion request with a 30-day response statement and no email prerequisite, removes the icon alpha channel, and adds fail-closed association/build checks without invented credentials. Full local validation passes 1,380/1,380; Capacitor sync/doctor and desktop/390px/412px production-component browser checks pass. Sol independently accepted the final local candidate after blocked-route and native-session corrections, with no P0/P1/P2 findings. On 2026-08-01 the existing non-production Supabase rehearsal schema was manually advanced through V132 after its automatic rebase failed on a duplicate V122 identity; the V131 rollback suite then passed after its role-restoration harness correction. Production was not changed. Publication remains blocked on owner-controlled Apple/Google signing, signed artifacts, live association HTTP 200 proof, production Turnstile `localhost`, store declarations/screenshots and physical-device acceptance. Evidence: `V131-STORE-PUBLICATION-READINESS.md`. |
| `NOTIFY-001` | Transactional in-app/Web Push for points, redemption, booking, package, and 3-day/1-day expiry; advanced promotions remain platform-mediated. | `IMPLEMENTED_UNVERIFIED` | Permission states, delivery records, dedupe, timezone, expiry scheduler, copy review. |
| `PROMO-001` | Screenshot 2026-07-29: owner uploads a real promotion image, enters factual offer details, uses a no-cost wording assistant with a clear CTA, edits the result, and explicitly publishes the company-wide offer. Customer programme gives the promotion visual priority while business identity and points remain compact. Each company receives ten first-published offer slots during the complimentary window ending 31 Oct 2026; unpublishing does not restore a slot, and no more than two active offers are shown at once. | `VERIFIED_LOCAL` | Focused v104 application/database tests pass 32/32, including maximum copy bounds, exact Singapore date boundaries, stable-receipt retry, tenant-switch isolation, priority `0`, owner-only fresh authoring, Super Admin zero-mutation denial plus exact-receipt support replay, non-null-branch rejection, draft/edit/unpublish finalization, and source/mirror/rollback coverage. Complete validation passes 1,034/1,034 plus quality, runtime-config, migration-manifest, canonical-order and static-build gates. Executable PostgreSQL acceptance passes, and a real two-session quota race ends at exactly 10 lifetime slots with one contender/receipt winner. Deterministic production-render evidence at 1440px, 390px and 412px records two cards, white headline contrast, wrapped cards/Terms/modal content, 44px CTAs and zero horizontal overflow in `docs/qa/evidence/V104-PROMOTION-VISUAL-ACCEPTANCE.md`. Sol independently accepted the refreshed local visual evidence. Still required before close: authenticated target owner upload/publish -> linked-customer refresh/CTA browser evidence, authorized migration/release, and production smoke. |
| `BILLING-001` | Automated subscription payment tracking; daily overdue notice and 14-day owner pause with representative contact. | `IMPLEMENTED_UNVERIFIED` | Stripe success/failure/refund/chargeback events, clock-bound notices, access pause/recovery. |
| `BILLING-PRICING-001` | Owner message 2026-08-01 plus owner correction 2026-08-04: sell one Nestly/Peekaa plan at SGD 148 monthly or launch SGD 99/month billed as SGD 1,188 annually (annual selected by default and SGD 168/month shown only as a truthful comparison price); include 1,000 customer profiles and charge SGD 10/month for each additional selected block of 1,000, with capacity increase available later. Do not charge per staff at launch. Show the included module overview, including owner-reviewed AI promotion copy assistance, before checkout. Every eligible V124/V162 subscription has one 30-day money-back-request window only where the accepted legal version grants it and never resets by plan/capacity changes. Sol review 2026-08-01 additionally requires pending-payment changes never report complete, exact uncertain recovery after webhook projection, no capacity-decrease bypass, authoritative removal of absent Stripe items, legacy-safe guarantee backfill, the same durable key/command on a lost-response retry, truthful terminal failure copy, and one immutable catalogue snapshot per command. | `VERIFIED_LOCAL` | V162 adds a forward migration for SGD 148 monthly base, keeps annual SGD 1,188 and +1,000 capacity at SGD 10/month or SGD 120/year, updates Terms legal digest to version 2026-08-04, and updates Stripe bootstrap/UI/docs. Focused V162 regression passes inside `tests/billing/v124-stripe-pricing.test.mjs`; combined billing/branding/platform/business targeted run passes 49/49. Build, quality, runtime-config, migration-manifest, canonical-migrations and diff-check pass. Production catalogue remains empty until real Stripe `price_...` IDs are created and confirmed. |
| `BILLING-TAX-001` | Owner message 2026-08-01: Nestly is not GST-registered, so company subscriptions must not charge or imply GST. Payments must appear in platform administration with the next payment date; daily overdue handling must pause only the business-owner workspace at exact day 14 and restore it after provider-confirmed payment without deleting customer value. Sol review 2026-08-01 found that existing subscriptions could retain automatic/default/item tax rates, an invoice-first webhook could bypass a term-dependent trigger, pending updates carried unsupported tax fields, and cached mutation responses could conceal later provider tax or requested-state drift. Owner message 2026-08-01 after Sol acceptance: “ensure that all task are completed and reviewed - if not please close the gap and push and deploy”; this is recorded as V125 release approval. | `VERIFIED_LOCAL` | V125 is merged and deployed. Production preflight and post-migration proof record zero tax rows, five validated constraints, four triggers and the exact no-GST policy; the hardened billing function is active and production UI says `GST not charged`. The live V124 catalogue has zero active rows, so Stripe checkout remains unavailable until owner onboarding, live Prices/secrets and provider/webhook acceptance are completed. The available Chrome session was signed out. Local evidence remains 37/37 focused, 49/49 combined and 1,348/1,348 plus build; Sol accepted the exact candidate. Evidence: `V125-NO-GST-NATIVE-READINESS.md`. |
| `LEGAL-IDENTITY-001` | Owner supplied ACRA business profile and `nestlyasia@gmail.com` on 2026-08-01. Public/commercial surfaces must identify NESTLY TECHNOLOGIES PTE. LTD. (UEN 202634502E), use the monitored business contact, and never expose the director's national identity number from the source document. | `SUPERSEDED` | The legal entity/UEN/privacy boundary and V124 evidence remain valid. The prior mailbox/product-brand requirement was superseded on 2026-08-02 by `BRAND-RENAME-001`, which now governs `admin.peekaa@gmail.com`, Peekaa and `peekaa.asia`. |
| `BRAND-RENAME-001` | Owner message 2026-08-02 supplied `Peeka Logo.png` and changed the product to **Peekaa**, public domain to `peekaa.asia`, and monitored email to `admin.peekaa@gmail.com`; replace the web logo, favicon and native/PWA app icons. The legal entity remains NESTLY TECHNOLOGIES PTE. LTD. unless separately changed. | `VERIFIED_DATABASE` | Baseline on `d993624` reproduced 373 files containing “Nestly”, 27 containing `nestly.asia`, 10 containing `nestlyasia@gmail.com`, and 14 containing `asia.nestly.app`. V134 projects Peekaa across current public/auth/share/legal/PWA/iOS/Android surfaces, uses deterministic opaque store icons, changes the unpublished native ID to `asia.peekaa.app`, preserves compatibility identifiers, and adds the paired legal/runtime migration. Current Chrome evidence covers signed-out, owner, platform and promotion surfaces at 1440/390/412px with source-bound metrics; native bundles are byte-equal to canonical app assets; the final-chain PostgreSQL 17 legal, runtime and V124 compatibility suites pass and roll back; the complete gate passes 1,395/1,395 plus build. Fresh Sol review, owner V134 approval, legacy-domain HTTP redirect, passkey re-enrolment, production/domain release and post-release proof remain. Evidence: `V134-PEEKAA-STRIPE-BRANDING.md`. |
| `BRAND-TURNSTILE-001` | Owner screenshots 2026-08-02: canonical Peekaa login still fails Cloudflare and the installed/update shell can still display Nestly. `peekaa.asia` is the current domain and every active/cached app surface must converge to Peekaa. | `BLOCKED` | V138 locally closes stale PWA brand convergence with immediate activation and network-first public assets. Live Turnstile `110200` remains because production hostname authorization requires an independently accepted V138 release and owner release approval before adding `peekaa.asia`/`www.peekaa.asia`; post-change token proof is still mandatory. |
| `STRIPE-LIVE-SETUP-001` | Owner message 2026-08-02: “for stripe - please help me to set up”; use the owner-provided private Stripe account/contact phone (redacted from the repository), account email `admin.peekaa@gmail.com`, public domain `peekaa.asia`, and the supplied Peekaa logo. Do not display the contact phone on receipts/Checkout. The reviewed launch offer remains SGD 148 monthly or SGD 1,188 annually, annual default, +SGD 10/month or +SGD 120/year per additional 1,000 customers, no staff charge, no GST, and one 30-day money-back-request window. | `REPRODUCED` | Production V124/V125 code, migrations and Stripe functions exist. Authenticated read-only audit on 2026-08-02 proves the account has no active status tasks, POSB payout ending 9997, and Cards/Apple Pay/Link/PayNow enabled, but the live catalogue and webhook list are empty; branding, public details, portal legal/redirect links and primary login/contact identity remain incomplete, payouts are manual, and Stripe's cancel-after-retries policy must be reconciled with Peekaa's day-14 workspace-pause contract. Checkout's support-contact display is currently off and must remain off. V134 locally rebrands the mutation-guarded catalogue script without changing amounts, cadence, compatibility keys or exclusive-tax behavior; its exact regression passes. No provider mutation occurred because owner-controlled login verification, Sol review and a subsequent V134 release approval remain mandatory. Evidence: `V134-PEEKAA-STRIPE-BRANDING.md`. |
| `STRIPE-CATALOG-CONVERGE-001` | Independent Sol V134 review 2026-08-02: rerunning the guarded catalogue setup over an existing V124 Product/Price set must converge customer-visible Peekaa names and descriptions or fail closed; it must not silently reuse Nestly-branded provider objects. | `VERIFIED_LOCAL` | The setup utility now updates exact existing V124 Product/Price presentation while preserving reviewed economics and compatibility lookup keys. `tests/billing/v134-stripe-catalog-convergence.test.mjs` proves the second run performs no POST and a conflicting Price aborts before any provider mutation. Live provider proof remains under `STRIPE-LIVE-SETUP-001`. |
| `BRAND-RUNTIME-FALLBACK-001` | Independent Sol V134 review 2026-08-02: no current notification, platform denied/error state, database-to-UI payload or exported filename may fall back to the retired Nestly product identity; the platform shell must use the supplied Peekaa logo. | `VERIFIED_DATABASE` | The current platform shell uses the supplied Peekaa logo and Peekaa export fallbacks. V134 replaces final V84 correction labels, V78 forfeiture copy, V102 package-policy notes, V106 revenue limitations and all six V109 sector evidence rows; it sanitises exact future billing notices/notifications, fails closed over any immutable pre-cutover fallback row and uses the current legal-operator no-GST error. Static tests and a disposable final-chain PostgreSQL 17 rollback suite pass; production migration proof remains release-gated. |
| `LEGAL-V134-VERIFY-001` | Independent Sol V134 review 2026-08-02: the Peekaa legal-manifest migration must have executable final-chain proof for historical acceptance preservation, old/new consent semantics, ACLs, replay/idempotency and V124 compatibility. | `VERIFIED_DATABASE` | On a disposable PostgreSQL 17 V129 clone, V130–V134 applied in order. `v134_peekaa_brand_legal.sql` and the corrected V124 rollback suite pass with exact active hashes, private ACLs, current consent write/replay/conflict, prior-consent reader compatibility and durable runtime-brand sanitisation, then roll back. Production was not changed. |
| `PROMO-PUSH-001` | Owner message 2026-07-31: individual companies need promotion pop-ups/push notifications, especially for expiring value and newly published promotions. | `VERIFIED_LOCAL` | Exact frozen migration, rollback-only disposable-branch acceptance, and full-suite evidence prove separate current consent, exact source/topic binding, durable campaign watermark, deterministic customer dedupe, inactive/future/media-less zero enqueue, generic-sync preservation only while current, and one bounded 15-minute activation/expiry sweep. The 390px dialog shows “Ending soon”, stays within 390px, and persists read/dismiss state through the server boundary. Provider receipt and production delivery remain before close. |
| `TEAM-ACCESS-DISCOVERY-001` | Owner screenshots and questions 2026-08-03: it is unclear where to add staff, whether a staff record needs user access, how to invite an additional user, and how to restrict the modules that user can read or edit. | `VERIFIED_LOCAL` | V148 surfaces **Team & permissions**, adds an explicit roster-only action, labels **No app access / Invite pending / App access active**, and retains owner-only Off/Read/Edit writers. `team access is discoverable and distinguishes roster-only from signed-in access` plus existing V74/V119 permission suites pass. Authenticated production browser proof remains gated. |
| `AUTH-QR-PERSISTENCE-001` | Owner screenshots and report 2026-08-03: scanning a Peekaa QR forces a new login even when the same person is already signed in; the signed-in session must remain until explicit sign-out. | `VERIFIED_LOCAL` | Root cause was the bare/www origin split. V148 permanently canonicalizes bare root and subpaths while preserving path/query, generates QR links on canonical www, retains `persistSession`/refresh and uses explicit local sign-out. `workspace sessions persist on the canonical origin and local sign-out is explicit` passes; later-day production-profile proof remains gated. |
| `CUSTOMER-PREVIEW-001` | Owner screenshot and report 2026-08-03: **Preview customer view** opens a real customer wallet and asks the owner to log in as a customer. Preview must be view-only without owner impersonation. | `VERIFIED_LOCAL` | V148 opens an owner-labelled read-only projection of published programme data with no customer authentication, balance, redemption or join writer. `owner customer preview renders published programme without customer authentication` passes across source/route contracts; authenticated desktop/390px production proof remains gated. |
| `PROMO-DISCOVERY-001` | Owner screenshot and question 2026-08-03: consolidated Grow navigation does not make it clear where to configure the existing business promotions that customers can see, including the two-offer customer limit. | `VERIFIED_BROWSER` | V148 adds one Promotions row/action in the existing Grow hub, keeps the v104 editor and retains the customer `slice(0,2)` cap. Focused V104 database/UI tests and desktop/390px/412px browser acceptance pass with two cards, 44px actions and no overflow. Production verification remains gated. |
| `DOMAIN-PERFORMANCE-001` | Owner screenshot and report 2026-08-03: `peekaa.asia` and a public booking QR remain on a skeleton for too long. Existing public, signup and workspace routes must become usable quickly and fail visibly rather than look frozen. | `VERIFIED_LOCAL` | Live measurement on 3 Aug shows canonical compressed transfer around 452 KB and 0.37–0.72 s warm total, while bare adds a redirect and varied to 1.37–4.94 s uncompressed. V148 canonicalizes the origin, preconnects the database, lazy-loads Chart/QR/scanner/spreadsheet libraries, and converts a stalled booking read into a timed retry state. `public booking exits its skeleton on timeout and offers retry` and optional-library contracts pass; production timing remains gated. |
| `LOYALTY-CATALOGUE-SOURCE-001` | Owner reward-editor screenshot 2026-08-03: live still says **Customer-facing name** and only exposes **Manual item or benefit**, despite the reviewed requirement to choose an existing product/service giveaway with visible selling price and company cost. | `VERIFIED_DATABASE` | V143 was never released; V148 forward-integrates the behavior onto V145. The editor now uses **Reward name customers see**, Custom or active product/service selection, editable selling price/company cost and cost-to-points guidance while retaining draft authority. V148 source tests and fresh 179-migration replay/rollback pass; independent review and production proof remain gated. |
| `LOYALTY-TIER-STARTER-001` | Owner screenshot and clarification 2026-08-03: tier setup should begin with a simple unpublished draft containing Gold, Platinum and Diamond so the owner fills thresholds/benefits instead of building every tier from scratch. | `VERIFIED_DATABASE` | V148 creates Gold/Platinum/Diamond atomically in the editable draft only, replays the same key byte-equivalently, returns `existing_tiers` for a fresh duplicate request and publishes nothing automatically. Database acceptance also covers denied outsider, boundaries, cloning and customer benefit projection. Independent review and production proof remain gated. |
| `ROLLBACK-COMPAT-001` | Current-chain rollback suites must seed approval-aware workspaces, causally ordered provider events, nullable attention, and protected media cleanup without bypassing the final authorization model. | `VERIFIED_LOCAL` | v92/v94/v95 and v106-v114 now return exit 0 and roll back with `ON_ERROR_STOP=1`; all five v95/v111-v114 two-session harnesses pass; local schema lint reports zero errors. Approval-first onboarding, workspace pause/recovery, Storage delete protection, finance/sales/refund/retention ACLs, tenant/branch isolation, replay and retry denials remain asserted. Independent Sol review is still required before this compatibility campaign can close. |
| `RELEASE-GAP-001` | Owner follow-up 2026-08-01: “Please close the gap and report back to me the status.” The non-production V129–V131 rehearsal reproduced a provider-paid self-service activation failure: the system activation trigger attempted to seed a draft loyalty programme without the locked owner identity required by the existing C45 writer guard. It also exposed two rollback-test harness defects around the stronger V125 no-GST trigger and V131 private-table denial. Independent Sol then found that a published bundle without Loyalty could still be offered and charged before draft seeding failed. Owner release approval received 2026-08-01: “RELEASE APPROVED: V132 release-gap closure — commit, push, apply V129–V132 production migrations, deploy reviewed functions and web production.” | `VERIFIED_DATABASE` | Only published Loyalty-capable sectors are offered or can be staged; deployment fails closed over any incompatible payment-pending row. After matching paid Stripe evidence, the locked workspace activates once and receives its owner-editable draft loyalty preset; the existing C45 owner/module guard and all browser grants remain unchanged; wrong price/cadence/capacity/tax/amount/subscription remains locked. V129/V130/V131 rollback suites pass against the non-production rehearsal branch with zero residue; full validation passes 1,381/1,381 plus build; Sol accepted with no P0/P1/P2 findings. Authorized production release now serves commit `fbb79882c85b04293686a86d85bf1c31ce77a40c`; all four production migration ledger rows are present; `stripe-billing-command` v5 is active with JWT verification; public shell/legal/build checks pass; and Vercel reported zero runtime errors in the first 30-minute scan. The row remains `VERIFIED_DATABASE`, not `VERIFIED_PRODUCTION`, until a configured live Stripe catalogue and signed provider payment exercise the exact activation path. |
| `OPS-CLOSURE-001` | Owner message 2026-08-01: “please proceed to close the gaps”, following the explicit question about outstanding work other than iOS/Android publication and Stripe. Close every reproducible in-repository release gap without representing external provider, legal, credential, or production-approval work as complete. Owner message 2026-08-02, after Sol acceptance: “please commit and push to live”, authorising the reviewed V133 commit, push, production migration and web deployment. | `IMPLEMENTED_UNVERIFIED` | V133 reproduces and closes the in-repository deletion-operations hole: System health now has a private paginated Super Admin queue with overdue/retention-due visibility, cross-operator claim completion, lost-response-safe actions and reviewed outcome controls; the database appends immutable exact-replay result snapshots, blocks privileged evidence mutation, preserves operator attribution and enforces bounded legal-retention review without deleting Auth, financial, security or audit data. Focused regressions pass 6/6; the isolated `migration-rehearsal` branch applied the final V133 shape and its rollback suite passed with outsider/service-role/table denial, 101-row pagination, retention review and zero Auth deletion; exact platform and customer components pass 1440px/390px reviewed/retained/deleted/refresh/error states with 44px controls and no overflow or duplicate request form. The accepted commit `c754d9d` was merged to `main` as `396c1a2`; production migration `nestly_v133_operational_closure` applied successfully with read-only RLS/grant/trigger/lifecycle checks, and `www.nestly.asia/api/build` identifies the exact production merge while the root, V133 bundle and three public legal/data pages return HTTP 200. Evidence: `V133-OPERATIONAL-CLOSURE.md`. The broader row is not complete: the authenticated all-role target sweep, fresh provider Auth/QR proof, target owner -> staff -> customer chain, automatic WhatsApp provider, alert/restore rehearsal, monitored support, counsel/DPO, Apple/Google signing and live Stripe remain open. The launch manifest still reports 17/17 P0 blockers without production-grade evidence; the scoped V133 release approval does not close them. |
| `READINESS-001` | Owner message 2026-07-31: “run a test to ensure all modules are ready and buttons are working - if too complex you need to streamline it.” Every role-visible destination must render an honest loading, ready, empty, denied, or recoverable error state; every visible enabled control must perform its labelled action once, and unavailable actions must be absent or truthfully disabled. Operational pages must expose one clear primary next action, with secondary/advanced controls progressively disclosed when equal-weight choices make the task hard to scan. | `VERIFIED_LOCAL` | Red-first audit found and V123 fixes hidden Setup Inventory navigation, an indefinite Setup loading state, a mouse-only staff row/mismatched instruction, false clipboard success, two-request bundle partial-write risk, stale or stranded report actions/exports, non-recoverable report errors, incomplete P&L exports, and 390px report/service containment. The strict source inventory covers 25 business routes, 9 platform routes and 269 literal controls; focused tests pass 9/9 and the complete gate passes 1,327/1,327 plus build. Production-component Chrome evidence covers a bounded set: sign-in, Setup, staff keyboard navigation, Daily Report, Profit & Loss and atomic bundle UI at desktop/390px. A disposable PostgreSQL 17 rehearsal applies 163/163 canonical migrations and the rollback suite proves exact replay, conflict, tenant/role denial and one-bundle/two-item atomicity. Sol 5.6 independently accepted the limited local remediation patch with no remaining code blocker. Exact evidence and limitations: `V123-MODULE-BUTTON-READINESS.md`. The required authenticated target sweep for every named role/module is missing, so focused browser artifacts do not qualify the overall objective as `VERIFIED_BROWSER`; `RELEASE-001` remains `CAPTURED` and production release is stopped. |
| `RELEASE-001` | Test every visible function/button with realistic owner, manager, front desk, customer, dual-role, platform roles and real sync; never overstate readiness. | `CAPTURED` | Every required matrix row verified; launch blocker manifest clear; Sol verdict; owner approval; post-release smoke. |
| `CONNECT-ONBOARD-001` | Owner messages 2026-08-02: “so each company will create their own stripe?”, clarified as each onboarded F&B/business using Peekaa as POS must receive its own customer payments, followed by “can we set up that infrastructure? then when business owner set up - how do we set up for them?” | `VERIFIED_LOCAL` | V142 adds an owner-only Customer payments surface, service-owned connected-account binding, and Stripe-hosted Express onboarding. One legal business owns one connection; branches share it by default. The second independent review found a test/live object isolation blocker; the candidate now passes the current mode into every setup/payment command, never retrieves a test object with live keys, permits only explicit test-to-live promotion, and rejects live-to-test downgrade. Clean 175-migration PostgreSQL 17 replay plus executable mode-promotion/tenant/idempotency acceptance pass. The full repository gate must be rerun and a new independent review plus real hosted Connect test onboarding remain required before `CLOSED`. |
| `POS-PAYNOW-001` | Owner messages 2026-08-02: a merchant customer must scan a QR for an exact sale such as SGD 10.00, cannot change the amount, and many business owners must each receive their own payments without a messy shared QR. | `VERIFIED_LOCAL` | V142 separates **External PayNow** from provider-verified **PayNow QR**. The corrected candidate binds the unconfirmed PaymentIntent before confirmation, locally renders Stripe's raw QR payload, reserves the same immutable exact-price evaluation, refreshes its 61-minute hold once when the QR is first observed, blocks a second staff tab from directly finalising it, rejects test-mode downgrade before provider creation, rejects mode/envelope mismatch, and prevents delayed refund API responses from downgrading signed refund events. Focused and production-component desktop/390px acceptance pass 20/20; a clean PostgreSQL 17 replay applies 175/175 manifest items and the behavioral SQL proves exact one-sale/one-10-point effect, reservation race denial, delayed QR activation, signed-refund precedence and tenant denial with zero fixture residue; the full gate passes 1,466/1,466 plus static build. Independent Sol acceptance and real Stripe test-mode QR/webhook/refund evidence remain required before `CLOSED`. |
| `RECEIPT-PRINT-001` | Owner message 2026-08-02: “print receipt.” | `VERIFIED_LOCAL` | V142 renders a merchant receipt only after the provider-backed attempt projects `succeeded`, includes merchant/branch/time/lines/total/method/safe reference, and adds a print-only action. The production Record-sale component passes desktop/390px selection -> locked QR -> provider projection -> receipt -> Print acceptance with zero service-phone copy and 44px-class visible controls. Second independent review and physical/PDF A4/80mm capture remain required before `CLOSED`. |
| `MULTIBRANCH-FOUNDATION-155` | Owner V155 request 2026-08-04: implement a safe multi-branch foundation with separate operational branch and reporting scope, permission-safe selected/all branch analytics, deduplicated retention, branch-scoped campaign preparation, and branch-enforced promotions. | `IMPLEMENTED_UNVERIFIED` | V155 adds a canonical server-side reporting-scope resolver, Dashboard/Customers/Merchant Insights V155 RPC usage, selected/all branch promotion availability, and a server-side promotion branch redemption guard. Existing loyalty, credit, package, ledger, expense, P&L, appointment, billing, authentication, tenant and role-authority semantics are preserved. Evidence target: `tests/business-ui/v155-multibranch-foundation.test.mjs`, V155 migration replay/build, and authenticated production multi-branch smoke after deployment. |
| `SELF-SERVE-CHECKOUT-UX-161` | Owner complaint 2026-08-04: “why my business create account does not have stripe pop up? - you need to build the UX”. Production check found zero active monthly/annual Stripe plan catalogue rows, so a real Stripe Checkout cannot open. The business signup UX must not promise immediate Stripe payment when provider prices are unavailable; after account creation/sign-in, it must explain exactly why Stripe did not open and make manual-help/bank-transfer/cash application capture the clear next step. | `IMPLEMENTED_UNVERIFIED` | V161 updates the account-creation copy and the signed-in setup fallback to state that Stripe Checkout opens only when plans are configured, otherwise the owner can submit business details for manual payment/help. The fallback records only an assisted application through the existing V159 RPC; it creates no workspace, invoice, receipt, charge, payment proof, or entitlement. Evidence target: `tests/business-ui/v130-self-serve-business-onboarding.test.mjs`, production active-catalogue read-only check, build, Sol review, and production browser smoke after deployment. |
| `IPAD-UX-CLOSURE-164` | Owner screenshots 2026-08-04: Staff Members must default to staff list with Add staff as a small upper-right action; Reports cards are too plain and hard to understand; Staff Performance winner cards have too much email text and need clearer visuals; Programmes should not show a persistent Continue setup action; Dashboard Performance/Understand sections must open usefully, show clearer KPI actions, include schedule glance links, and avoid redundant Merchant Insight CTAs. | `VERIFIED_LOCAL` | V164 updates the local candidate only: Staff Members now uses Staff list / Invites & access tabs with Add staff in the toolbar; Reports is labelled Business Insights with stronger visual category cards; Staff Performance winner cards use compact visual ranking treatment; Programmes removes the persistent Continue setup header action; Dashboard collapse storage was versioned so first V164 load expands Performance and Understand, adds schedule glance links, changes branch-loading copy to non-broken scope labels, adds metric action labels and removes the redundant revenue-started sales-report CTA. `tests/business-ui/v164-ipad-ux-closure.test.mjs` passes 8/8, `npm run build` passes, and `git diff --check` passes. Browser and production proof remain blocked by release governance; no commit, push, deployment, schema change or production mutation has been performed. |
| `IOS-NATIVE-SHELL-165` | Owner message 2026-08-05: Xcode and the iOS simulator are installed; inspect whether an iOS, Capacitor, Expo, React Native or Swift project already exists; do not create an unrelated repository or change production; prepare the smallest maintainable iPhone app implementation using the existing web application where possible; generate/build the Xcode project against the installed iPhone simulator; provide the exact Xcode project/workspace path; preserve current Vercel, Supabase, authentication and payment behaviour; commit and push to a separate branch but do not submit to the App Store or deploy production. | `VERIFIED_LOCAL` | The repository already contains the canonical Capacitor wrapper (`capacitor.config.ts`, `ios/App/App.xcodeproj`, Android mirror and `docs/mobile/capacitor-wrapper.md`). V165 keeps the web app as the only source, syncs `dist/mobile`, does not hard-code a production web URL in Capacitor, pins Xcode's Capacitor SwiftPM package resolution, and builds the existing iOS project for the installed iPhone simulator. Evidence: `tests/mobile/v165-ios-capacitor-shell.test.mjs`, `npm run mobile:store:validate`, `npm run build`, `npm run mobile:sync`, and `xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Debug -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.5' build`. Commit/push remain subject to repository release governance; no production deployment, App Store submission, Supabase change, payment change or production secret change is part of this task. |
| `OUTSTANDING-VERSION-INTEGRATION-166` | Owner inventory and release instruction 2026-08-05: review the 13 named V117/V142/V143/V145/V148/V154/V156/V157/V158/V161/V162/V163/V165 local-only items, publish every genuinely outstanding change, and do not report completion until the exact production build is verified. | `VERIFIED_PRODUCTION` | Reconciliation started from production build `215e5a90b204d08f8e628851a1595dcc09264cae`, not stale local/main tips. V154/V156/V157/V158/V161/V162/V163 were direct production ancestors; V148 was patch-equivalent; V117 and V142 canonical migrations were byte-identical; V143 was forward-integrated through V148; V145/V144 was superseded by the reconciled production lineage. V165 was the sole absent clean descendant and was carried into V166. Sol accepted the exact candidate with no P0/P1/P2 findings. Feature commit `e5520e768c8a5d1a3dde5949b552541c9d491613` was promoted through PR #18 as main commit `6db41b0ce0540fc6c2a562df11bf750fd4355446`; production `/api/build` matched exactly. Customer/business/admin routes passed 1440x1000 and 390x844 production browser smoke with no overflow, overlay, browser error or console failure. The production migration ledger was checked read-only and V166 applied no migration. Focused suites pass 140/140 across two runs, quality/build/mobile/simulator/diff gates pass, and comparison with V164 proves zero new full-suite failures and 11 resolved; 65 pre-existing historical failures remain explicitly open as repository debt. Evidence: `docs/qa/evidence/V166-OUTSTANDING-VERSION-RECONCILIATION.md`. |

## How to add a new owner report

Add a row immediately, even when diagnosis is unknown. Then add a matching
traceability row with:

`ID | source | acceptance | owner config | staff action | customer projection |
fixture | negative states | automated evidence | browser evidence | DB evidence |
production evidence | state`

When one screenshot contains several failures, create separate IDs. A single
visual artifact may support several rows, but each row retains its own
acceptance criteria.
