# Nestly traceability matrix

Last consolidated: 2026-08-01

This is the mandatory planning artifact before application code changes. The
state is deliberately conservative: implementation presence does not prove the
journey.

## Working matrix

| ID | Owner configuration | Staff operation | Customer/platform projection | Realistic fixture | Negative and recovery states | Current evidence | State |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AUTH-001` | Customer chooses password and consents during account creation. | N/A | Sign in by phone/password or passkey; OTP only create/recover. | `CUS-MEI` on supported and unsupported devices. | Wrong password, expired OTP, resend, reset reload, passkey cancel, unsupported browser. | Existing auth tests not yet reconciled to all device acceptance criteria. | `IMPLEMENTED_UNVERIFIED` |
| `AUTH-FORWARD-001` | Reset local data, create a new approval-first `SPA-GLOW` tenant, and create a new `CUS-NEW` phone/password account with the current legal/marketing choices; no prior login is reused. | Owner activation remains blocked before platform approval; after approval the assigned owner and branch-scoped staff operate only within current role/module/branch grants. | Account creation consumes one OTP, the next logout/login uses phone and password without sending an OTP, saved profile/consent survives refresh, the preserved QR join intent creates exactly one relationship, and later staff activity is read from the same persistent ledger by the customer portal. | `SPA-GLOW`, `CUS-NEW`, duplicate-name `CUS-LEE-A`/`CUS-LEE-B`. | Unapproved firm, disabled module, wrong branch, denied role, duplicate scan, duplicate name resolved by phone, lost response/retry, logout/login, refresh/reconnect, desktop and 390px. | Historical test accounts are excluded by owner ruling. Fresh password sessions for the forward customer and Orchard front desk drove the rendered 1,080 -> pending QR -> server-confirmed scanner receipt -> 80 points and newest `-1000` History chain. The completed intent and canonical operation were read back from local Postgres, and the completed customer state was rendered at desktop and 390px. `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md` records exact hashes and artifacts. Provider-side one-OTP accounting and native-device passkey acceptance remain separate `AUTH-001` evidence. | `VERIFIED_DATABASE` |
| `JOIN-001` | Owner enables new signups and publishes branch/business QR. | Staff may display/share the issued QR. | Scan preserves join intent through auth and creates one business link. | `SPA-GLOW` plus new/existing customer. | Disabled link, stale QR, duplicate scan, refresh during auth, wrong branch. | Existing join implementation; target browser/DB chain unverified. | `IMPLEMENTED_UNVERIFIED` |
| `CUSTOMER-001` | Owner publishes logo/programme and enabled benefits. | Staff creates ordinary customer activity. | Compact selector and selected programme home show only real enabled content. | `CUS-MEI` linked to 0, 1, and 6 firms. | Empty, missing logo, disabled benefit, slow load, mobile overflow. | Customer UI suites exist; cumulative visual criteria unverified. | `IMPLEMENTED_UNVERIFIED` |
| `CUSTOMER-002` | Owner defines earn rules and catalogue. | Staff records sale/reversal/package use. | Full history and balances match source ledgers after reload. | `SPA-GLOW` sale + reversal + package; `CAFE-HARBOUR` stamp. | Lost response, double submit, correction, cross-branch transaction. | Ledger/UI suites exist; exact record comparison outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `CUSTUX-001` | Owner publishes normal programme; no relationship-removal setup is required. | N/A | Customer sees only task-relevant controls; destructive unlink is absent from the main programme page. | `CUS-MEI` with one and six programmes. | Keyboard focus, mobile overflow, direct legacy action absence. | v103 control audit plus `v103-programme-clarity-ui` proves primary control removals and dedicated bell route; authenticated browser artifacts outstanding. | `VERIFIED_LOCAL` |
| `CUSTUX-002` | Owner publishes valid presentation and programme content. | Ordinary activity keeps loading independently. | Programme either loads coherently or shows a precise recoverable degraded state without contradicting visible content. | `SPA-GLOW` with presentation success, missing optional presentation, and RPC error. | Missing migration/function, active-branch absence, permission denial, network retry, refresh. | v103 migration adds verified `business.id`; rollback SQL proves isolation/ACL; UI validates UUID before both RPCs and degrades optional presentation only. | `VERIFIED_LOCAL` |
| `CUSTUX-003` | Owner configures no tier, then a real two-tier programme. | Staff creates points/visit progress. | One readable balance; tier card only for real progress; clear next reward. | `CUS-MEI` at 70,576 points and ordinary 1,250 points. | Very large values, no reward, no tier, completed tier, 390px viewport. | v103 helper/DOM regressions prove compact formatted single balance and truth-based tier rendering; authenticated viewport capture outstanding. | `VERIFIED_LOCAL` |
| `CUSTUX-004` | Owner has ordinary transaction and points history. | Staff creates purchase, correction, redemption, and package use. | One History section contains transactions/points with progressive disclosure and full detail. | `SPA-GLOW` 20+ mixed events. | Empty, error/retry, load more, reversal relationship, mobile long descriptions. | v103 DOM regression proves collapsed History owns both sections and successful empty states have no false recovery CTA. | `VERIFIED_LOCAL` |
| `CUSTUX-005` | Owner enables QR redemption and publishes eligible/ineligible rewards. | Staff later scans the prepared QR. | Each eligible reward shows Redeem now; state-specific copy replaces dead or misleading actions. | `CUS-MEI` sufficient and insufficient balances. | Disabled QR, expired/ended, pending intent, double tap, already redeemed, wrong branch. | v89/v103 regressions pass classic/catalog eligibility, state copy, idempotency key, pending intent QR, cancel, and merchant scanner access; target scan outstanding. | `VERIFIED_LOCAL` |
| `REWARD-PROJECTION-001` | `SPA-GLOW` has an active classic programme (10 points per SGD, 1,000 points for SGD 10), customer redemption enabled, no versioned catalogue reward rows, and effective Loyalty access at Orchard. | Orchard front desk with Loyalty and Till write scans and confirms the customer's pending classic-reward QR. | At 1,080 points the linked customer sees one **Redeem 1000 points** reward with **Redeem now**; prepare creates one pending intent and spends nothing; staff confirmation creates one completed canonical redemption operation and one -1,000 ledger row; refresh shows 80 points, **More points needed**, and one newest History redemption. | Forward-only `SPA-GLOW` / Glow Atelier Spa, Orchard branch, `CUS-NEW` Mei Lin Fresh. | v94 firm Loyalty disabled (raw sector array still contains Loyalty), inactive programme, insufficient balance, redemption disabled, expired pending intent, duplicate confirm, denied role, branch Loyalty disabled, wrong branch, lost response/retry, refresh, desktop and 390px. | v117 DB regression `db/tests/v117_effective_loyalty_boundaries.sql` proves firm-disabled capability/action/preparation and zero mutation, then enabled completion/replay. `tests/customer-modules/v117-effective-loyalty-boundaries.test.mjs`, `tests/customer-wallet/v116-classic-reward-projection.test.mjs` and the full 1,284-test gate pass. Current browser proof shows 1,080 and **Redeem now**, the pending no-spend QR, the staff server receipt, then 80 and newest `-1000` History after customer sign-in/refresh at desktop and two true 390 x 844 viewports with zero horizontal overflow. Exact hashes/artifacts: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol review remains. | `VERIFIED_DATABASE` |
| `REDEEM-SCANNER-001` | The fresh `SPA-GLOW` owner grants Orchard front desk Till/Sales/Loyalty write and creates a current selected Orchard branch; no historical login is accepted as evidence. | From Quick Earn, front desk opens **Scan customer QR**, scans or pastes the fresh linked customer's pending classic-reward QR, and the client sends business, selected branch, token, and stable idempotency key to the current branch-effective scanner RPC. | Preparation does not spend; one successful Orchard confirmation produces one completed canonical redemption operation and one -1,000 ledger row; customer refresh shows 80 points and newest History redemption; replay produces no new economic effect. | Forward-only `SPA-GLOW`, Orchard Road and assigned comparison branch, front desk, `CUS-NEW` Mei Lin Fresh. | Missing branch is blocked before RPC; assigned-but-Loyalty-disabled branch is denied server-side without mutation; obsolete v89/v93 execution is denied to authenticated and anonymous clients; wrong/unassigned branch, denied role and expired token do not mutate; replay/lost response reuse the stable key; desktop, 390px, refresh/reconnect. | v117 exact-branch DB acceptance compares intent, points-ledger, canonical-operation and credit counts around the denied direct RPC, proves one enabled-branch completion and replay, and verifies obsolete execution is revoked. Current Orchard front-desk browser proof shows Quick Earn -> scanner -> exact server receipt; local Postgres ties that receipt to idempotency key `v117:c4ce6613-db60-42d5-81cd-26db7143cccf`, and customer refresh shows the persisted result. Exact tests/hashes/artifacts: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol review remains. | `VERIFIED_DATABASE` |
| `BOOKING-001` | Owner toggles customer booking and configures services/staff/branches. | Front desk creates and amends appointment by phone-distinguishable customer and named staff. | Customer sees only enabled booking actions and updated appointment. | `SPA-GLOW`, duplicate Lee names, Orchard/Tampines. | Disabled booking, no staff, conflict, denied role, reschedule refresh. | Booking tests exist; full owner->staff->customer proof outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `APPT-BLOCK-001` | Owner keeps Appointments enabled at Orchard, assigns Chen Wei and Aisha Rahman there, and grants the acting owner/front desk Appointments edit access. No separate blocked-time module or navigation is created. | In Appointments Day view, an authorized user opens **Block time**, chooses Chen Wei, 31 July 2026 14:00–15:00 Singapore time, records “Supplier training”, saves once, reloads, then removes it. The visible block suppresses every start whose service plus before/after buffers overlaps it; booking and rescheduling RPCs reject the same interval server-side. | N/A: this is staff-only operational availability. Existing customer booking history is unchanged; no customer-facing blocked-time reason is exposed. | `SPA-GLOW` Orchard: Chen Wei and Aisha Rahman; Spa Ritual 60 with its configured buffers; one existing 15:00 booking. | Appointments disabled/read-only; wrong or unassigned branch; inactive staff; empty team/day; invalid/cross-day/>24h interval; duplicate and overlapping block; appointment/buffer collision; same-key lost-response retry; concurrent writers; load/create/delete failure and retry; refresh/reconnect; desktop and 390px horizontal day view. | V120 local regressions cover the application/migration/docs contract. `db/tests/v120_staff_blocked_times.sql` passes transactionally on disposable PostgreSQL 17 for ACL, replay, overlap, buffers, missing-hours, redaction, direct-trigger, delete, and audit behavior. `v120_staff_blocked_times_concurrency.sh` requires a separately named `v120_concurrency` database, leaves immutable fixture evidence intentionally, and requires the caller to drop that whole database. A newly created dedicated clone ran real block-vs-block and both block/appointment writer orderings, finished `2|1|2|2`, and was then dropped with database count zero. Deterministic browser evidence covers normal, authoring, read-only, empty, load/create/delete error, desktop, and 390px states; the corrected read-only capture has zero create/remove controls, no literal-template artifact, and no horizontal overflow. Sol/Luna accepted the exact local candidate. Authenticated target-browser/database proof remains open. Exact evidence: `V120-STAFF-BLOCKED-TIME-ACCEPTANCE.md` and `SOL-V118-V121-INDEPENDENT-REVIEW-2026-07-31.md`. | `VERIFIED_LOCAL` |
| `OPSUX-001` | Owner keeps existing module policy; no duplicate setup is introduced. | Owner/front desk starts from permission-aware Record sale, Add appointment, Find customer, or Set up rewards cards. | N/A | Salon/spa owner, front desk, read-only manager. | Disabled module, read-only role, narrow phone. | V118 dashboard regression passes and proves individually gated tasks, collapsed performance detail, and one-column narrow layout. Authenticated role browser proof remains open. See `V118-TASK-FIRST-UX-ACCEPTANCE.md`. | `VERIFIED_LOCAL` |
| `OPSUX-002` | Owner has named team members and appointments. | Day view presents named team columns, recorded schedule windows, the current-time marker, and observable booking facts; Week/List are secondary. | N/A | Two-person salon day with booked, empty, and unassigned work. | Empty team, empty day, staff filter, mobile overflow. | V118/V120 local regressions pass and prove default Day, named time-grid columns, time/customer/service/duration/status, current Singapore-time marker, horizontal mobile scrolling, and non-misleading empty copy. Deterministic 390px evidence exists; authenticated mobile viewport proof remains open. | `VERIFIED_LOCAL` |
| `OPSUX-003` | Owner has appointment write access and current branch context. | New appointment form stays hidden until requested; clicking inside a recorded work window snaps to 15 minutes and pre-fills date, staff, and exact time; branch breaks reject creation. | N/A | Two-person salon day. | Denied write, missing schedule, staff leave, branch break, closed form, branch change. | V118/V120 regressions pass and prove progressive disclosure, exact clicked-time prefill, recorded-hours gating, leave/off state, branch-break exclusion, and durable time-specific block handling. Authenticated branch-change persistence remains open. | `VERIFIED_LOCAL` |
| `OPSUX-004` | Owner publishes classic points at 10 points per SGD and effective Loyalty is resolved at the exact sale branch. | Olivia completes `CUS-MEI`'s SGD 60 Spa ritual 60 through the explicitly labelled **Complete & checkout** action. V47 remains the status writer and a second identical call is a replay. | With Orchard Loyalty `rw`, the appointment, one appointment-linked sale and one +600 earn persist; after refresh `customer_get_wallet`, `customer_get_business_summary`, and `customer_get_loyalty_details` all report the same 600 balance and one earn. Existing balances remain persistent when Loyalty later becomes unavailable; current disabled/read-only policy still governs whether the customer can read it. | `SPA-GLOW`: Glow Atelier, Olivia Tan, Farah Noor, `CUS-MEI`, Orchard and Tampines, Spa ritual 60 at SGD 60. | Same-call retry/double click; firm `disabled`; branch `disabled`; branch `r`; wrong-branch/unassigned front desk; no duplicate sale/earn; preservation of prior balance; refresh. Reversal and authenticated browser/mobile remain separate open evidence. | `db/tests/v121_appointment_completion_customer_projection.sql` failed on the V120 predecessor with a hidden +600 earn under firm-disabled Loyalty, then passed on local PG17 after V121 for the enabled chain, all three effective-policy negative modes, replay, branch denial, persistent counts and refreshed customer-safe projections. Static/canonical/manifest checks pass and Sol/Luna accepted the exact local candidate. Exact evidence: `V121-APPOINTMENT-COMPLETION-LOYALTY-BOUNDARY-ACCEPTANCE.md` and `SOL-V118-V121-INDEPENDENT-REVIEW-2026-07-31.md`. Authenticated target database/browser and reversal proof remain open. | `VERIFIED_LOCAL` |
| `OPSUX-005` | Owner records branch hours, breaks, staff hours, leave, service duration and before/after buffers. | Day calendar offers explicit 44px start-time buttons only where the selected service fully fits; the exact chosen time, staff and service prefill the appointment form. Adjacent 15-minute bookings remain visually distinct. | N/A | Two-person spa with 30/60/90-minute variants, a lunch break, leave and adjacent bookings. | Missing branch weekday means closed; conflict, buffer collision, break, off-day, refresh, keyboard selection and 390px horizontal team view. | V119/V120 local regressions pass for recorded-hours, break/leave, service-buffer, adjacent-booking, exact prefill, missing-hours fail-closed, refresh projection, and 390px horizontal layout. The visual artifact is deterministic rather than an authenticated target journey. | `VERIFIED_LOCAL` |
| `OPSUX-006` | Owner changes a staff role, removes a staff member, changes module permissions or applies a template. | The next route/branch load fetches a fresh effective module projection so it cannot retain the former persona. | Disabled or newly enabled customer-facing capabilities follow the same effective policy rather than raw module arrays. | Owner, manager and front-desk personas at two branches. | Role downgrade, removal, template apply, branch switch, retry and reload. | V119 local regression passes and models a second session seeing a downgrade because the route loader no longer retains an indefinite browser projection cache. Authenticated multi-session role/template evidence remains open. | `VERIFIED_LOCAL` |
| `REPORT-CORE-002` | Owner records branch hours/breaks, staff hours, leave, and one-off staff blocks. | Busy-time reporting compares booked minutes with scheduled capacity inside branch opening hours, minus the union of breaks and persisted blocks and with full-day leave removed; missing branch hours are unknown/closed and over-capacity is shown truthfully above 100%. | N/A | Salon with split shifts, lunch, supplier training, and deliberate overbooking. | Closed weekday, missing hours, overlapping break/block, leave, inactive staff, branch switch, retry and >100% utilisation. | V119/V120 calculation regressions pass for active staff, merged break/block intervals, full-day leave, missing-hours fail-closed behavior, and truthful over-capacity arithmetic. Authenticated report/database comparison remains open. | `VERIFIED_LOCAL` |
| `REPORT-CORE-001` | Existing reporting data and branch scope remain unchanged. | Owner starts from money, utilisation, returning customers, or team questions; detail is revealed only when requested. | N/A | Low-data and established SME. | Empty range, no module, read-only role, mobile, export failure. | V118 regression passes and proves four decision entries and collapsed detailed money report. Authenticated empty/error/mobile browser acceptance remains open. | `VERIFIED_LOCAL` |
| `TEAM-001` | Owner configures team and branch assignments. | Scheduling UI must distinguish bookings from authoritative working hours, leave, breaks, and blocks. | N/A | Two-person salon with leave and breaks. | Missing hours, overnight shift, cross-branch staff, stale schedule. | V118/V120 implement persisted staff hours, staff off-days, branch hours, branch breaks, staff-wide one-off blocks, cross-branch redaction, and missing-hours fail-closed behavior. Overnight schedule acceptance and authenticated stale/branch/database evidence remain open. | `IMPLEMENTED_UNVERIFIED` |
| `REDEEM-001` | Owner enables QR redemption and reward. | Staff scans and confirms pending QR in Quick Earn. | Customer value changes once and receives completion history/notification. | `CUS-MEI` with sufficient/insufficient points. | Expired QR, double scan, concurrent scan, wrong firm/branch, cancel. | Redemption tests exist; production-shaped concurrency proof outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `ONBOARD-001` | Platform sets sector template and approval decision. | Consultant manages only assigned cases. | Owner activation is unavailable before approval; approved firm is visible in platform records. | `PLAT-PENDING`, `PLAT-WON`. | Duplicate request, rejected, stale approval, admin/sales scope. | Platform controls exist; complete approval boundary unverified. | `IMPLEMENTED_UNVERIFIED` |
| `MODULE-001` | Super Admin edits sector, firm, or branch effective modules. | Owner limits staff within effective set. | Hidden modules are absent and direct routes/actions are denied. | `SPA-GLOW` with branch override; `EMPTY-MERCHANT`. | Owner escalation, branch switch, stale client cache, direct URL/API call. | Module tests exist; cumulative branch/role matrix outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `MODULE-PROJECTION-001` | Current super-admin writer sets firm and branch module modes for newly approved `SPA-GLOW`, including firm-disabled Gift cards/Inventory and an assigned branch comparison. | Newly created owner and Orchard front desk load branch-effective navigation and Quick Earn, then attempt direct writer calls at enabled and disabled branches. | Disabled modules are absent after login/reload; an unassigned front desk receives no projection; branch-only enabled work is reachable only at that branch; direct new gift issuance is denied at a disabled branch; a synthetic pre-existing SGD 50 gift liability remains transferable once to customer credit without reopening issuance. | Fresh `SPA-GLOW`, Orchard/comparison branches, front desk, synthetic existing SGD 50 gift liability. | Stale cached persona, resolver failure, unassigned staff, firm/branch inherit/disable/r/rw, owner escalation, direct route/API, reload and branch switch, zero-liability and existing-liability states. | v115-v117 database regressions prove branch-effective projection/writer denial, no unassigned projection, branch-only isolation, zero-mutation disabled gift issuance, and one SGD 10 liability-to-credit transfer leaving SGD 40. Same-key replay creates no second liability reduction, credit or immutable receipt. Current staff browser proof covers the exact Orchard Quick Earn scanner; disabled-module UI remains regression evidence. Exact tests/hashes/artifacts: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol review remains. | `VERIFIED_DATABASE` |
| `PLATFORM-001` | Super Admin configures roles, consultant assignment, rates, and billing. | Platform roles operate within assigned scopes. | Firm/branch/customer reports and onboarding/billing/commission state are correct. | Platform CRM fixtures in `REALISTIC-FIXTURES.md`. | Unassigned firm, inactive staff, refund/GST/chargeback, overdue recovery. | Several platform migrations/UI tests; consolidated proof absent. | `IMPLEMENTED_UNVERIFIED` |
| `ADMINBOOT-001` | N/A | Super Admin opens `/admin` directly or returns after an app update. | A meaningful sign-in, loading, recovery, or authorized console state is visible immediately; the route never presents an unexplained blank canvas while session/runtime work is pending. | Signed-out, authorized, and denied platform identities on current and one-version-stale PWA shells. | Slow/offline auth, stale service worker, update waiting, missing platform script, route rejection, reload/back-forward, 390px and desktop. | Production reproduction identified a frozen-helper Proxy invariant and an un-awaited async renderer. The local route paints before session resolution, awaits/catches render failure, and visibly rejects missing/invalid platform modules. Executable harnesses cover frozen CUI, missing module, query routes and rejection. Deterministic real-browser evidence covers ready and denied states, refresh/deep links, 1440px and 390px with zero horizontal overflow. Production smoke is intentionally pending release. | `VERIFIED_BROWSER` |
| `ADMINIA-001` | Super Admin retains every existing platform capability and permission boundary; sector defaults and firm/branch exceptions remain distinct. | Super Admin/Admin/Sales staff reach daily work through task-oriented groups; an operator searches one bounded directory, opens Firm 360, and manages module exceptions in one grouped workspace. | Five primary destinations preserve all authorised deep links; Today has one action per exception; Firms hands scope to Reports; Finance and System health have distinct responsibilities; module policy exposes one Reset and one Save instead of per-module Edit buttons. | Submitted spa application, blocked spa onboarding, overdue billing firm, payable commission, failed reconciliation, Glow Atelier with two branches/164 customers/19 governed modules, and role-scoped users. | Read-only/no access, empty or partial data, RPC failure, mobile overflow, forged firm route, reload/back, translated long labels, explicit pagination. | Focused v105/localization/migration suites pass **80/80** and the complete repository suite passes **1,074/1,074**, with build and validation green. The Today-coverage increment passes **33/33** focused tests: v88 attention row 251 is loaded through its cursor; delegated Admin and Sales exhaust the snapshot-frozen v105 cursor and surface actionable row 51; all cap-only/summary domains expose partial coverage at their boundary; RPC-unavailable or guard-stopped coverage suppresses false caught-up copy; exact totals remain distinguished from lower-bound derived KPIs; and the visible queue stays bounded to 12 rows. Production-component browser evidence covers super-admin/admin/sales/denied roles, a forced access-check failure, Today desktop/mobile, onboarding reload, Firm 360, and the grouped module manager: 6 groups, 19 selectors, one Reset, one Save, zero per-module Edit buttons, zero visible sub-44px action/control targets, and no horizontal overflow. Exact evidence is recorded in `V105-SUPER-ADMIN-UX-AUDIT.md`. Authenticated production smoke and independent reviewer acceptance remain release-gated. | `VERIFIED_BROWSER` |
| `INTEL-001` | Platform chooses sector/company/branch report scope. | Assigned consultant generates and reviews intelligence. | Report explains source window, metrics, confidence, and actionable advice; owner route denied. | `SPA-GLOW` 180-day history and `CAFE-HARBOUR`. | Sparse data, branch filter, unassigned consultant, owner direct URL. | Reporting tests exist; consultant acceptance and provenance outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `QUICK-001` | Owner enables catalogue-led checkout and eligible modules. | Staff searches by phone, selects variant/entitlement, confirms or corrects. | Customer transaction/points/package state updates consistently. | `SPA-GLOW` variants and duplicate names. | Catalogue off, no results, denied role, double tap, timeout/retry, reversal. | v102 covers package paths locally; broader journey outstanding. | `IMPLEMENTED_UNVERIFIED` |
| `QUICK-002` | Owner keeps Gift Cards independently enabled or disabled; Quick Earn configuration has no gift-card option. | Owner/front desk opens Quick Earn and sees catalogue sale, packages/vouchers/memberships and pending reward scan only; gift-card issue/apply/redeem remains in the permission-gated Gift Cards route. | Existing customer gift value is unchanged; no Quick Earn action can mutate it. | `SPA-GLOW`, `CUS-ARUN`, existing SGD 50 liability. | Gift Cards enabled/disabled/read-only, direct legacy handler, denied role, selected customer with gift value, refresh, desktop/390px. | `tests/business-ui/v122-owner-seven-workflows.test.mjs`: “Quick Earn contains no gift-card issue or redemption controls”; v115/v117 regressions preserve dedicated Gift Cards and liability authority; full 1,318-test validation passes. | `VERIFIED_LOCAL` |
| `CALENDAR-001` | Owner configures Orchard hours, Chen/Aisha schedules, breaks, leave, blocks, service duration/buffers and Appointments write access. | In Day view, click/tap Chen 10:15 to open the hidden form prefilled with Chen/date/10:15; save one appointment; then independently create an eligible Aisha appointment from her column. | Customer booking/history shows the persisted appointment when linked; staff calendar and list agree after refresh. | `SPA-GLOW` Orchard, Chen Wei, Aisha Rahman, `CUS-MEI`, Spa Ritual 60. | Occupied time, buffer collision, branch break, leave, one-off block, closed/missing hours, read-only/front-desk branch denial, double tap/lost response, refresh/reconnect, keyboard, desktop/390px. | Exact frozen migration plus rollback-only `db/tests/v122_owner_seven_workflows.sql` passes on development branch `wtegnefsgnyxhflzizcu` and reads back Alina/Maya exact staff IDs. The shipped `appointmentsPage` production-component harness proves desktop click/prefill and save/refresh with one appointment under each named column in `calendar-click-desktop-two-staff.png` and `calendar-save-refresh-desktop-two-staff.png`; `calendar-click-mobile-390.png` proves Maya/31 July/13:00 prefill with `clientWidth=scrollWidth=390`. `tests/business-ui/v122-owner-seven-workflows.test.mjs` locks the slot wiring and mobile grid containment. | `VERIFIED_LOCAL` |
| `GROW-001` | Owner selects guided recommendation and edits visual overview. | Manager can review but permissions constrain publishing. | Published programme matches preview/customer result. | `SPA-GLOW` and `CAFE-HARBOUR` recommendation data. | Empty catalogue, sector mismatch, range > benchmark, refresh, draft conflict. | v97-v102 focused and full local suites passed in the phase worktree; browser persistence pending. | `VERIFIED_LOCAL` |
| `REWARD-OVERVIEW-001` | Owner publishes one earning model/rate, a classic redemption promise or sequenced catalogue rewards, and an optional birthday benefit. The overview reads only the published version; edits create/resume the protected draft. | Owner sees one ordered **Rewards overview** with the earn rule, every active reward, paused/archived reward disclosure and birthday configuration. Clicking an individual stable-ID card opens that exact reward editor and retains focus there; clicking birthday opens its exact birthday editor. Saving stays draft-safe until explicit publish, then refresh reads the published values. A read-only manager may review but receives no writer. | Customer preview/portal mirrors only the published earning model, currently applicable reward thresholds/benefits and birthday eligibility; classic leftovers, paused programmes, future rewards and ended claim windows are explicitly unavailable according to the server-authoritative snapshot time, and no draft or invented reward appears. | `CAFE-HARBOUR`: SGD 5 per stamp, reward at stamp 5 and stamp 10; `SPA-GLOW`: 10 points per SGD, classic 1,000 points -> SGD 10 plus active/disabled Birthday Glow; duplicate display-name rewards with different UUIDs prove identity routing. | No programme; paused programme; empty rewards; disabled birthday; classic vs points-catalogue vs stamps; future/ended claim window; duplicate name/threshold; archived item; many/long names; draft absent/present; read-only manager; Loyalty disabled/read-only; load failure/retry; save conflict/lost response; refresh/reconnect; keyboard/touch; desktop/390px/no overflow. | Red-first `tests/business-ui/reward-overview-owner.test.mjs` (6/6) plus V122 workflow (7/7). `db/tests/v127_rewards_overview_birthday_reader.sql` passes server `as_of`, owner/read-only manager plus denied/cross-tenant/anon/raw-table/draft denial on a disposable local Supabase clone. Chromium production snapshot-adapter source `0182cb1…` passes 1440px exact second-UUID interaction with retained editor-input focus, 390px Birthday Glow/no overflow/44px cards, and 390px manager zero-writer state with one birthday RPC and zero birthday-table reads. Sol independently accepts this exact candidate with no P0/P1/P2 findings. Artifacts and exact limits: `REWARD-OVERVIEW-OWNER-ACCEPTANCE.md`. Exact reward save/publish/customer mirror, conflict/lost-response and production remain pending. | `VERIFIED_BROWSER` |
| `PROFIT-AI-001` | Owner records cost and price for a real catalogue item/service and selects a reward objective. | Owner sees profit before reward, estimated reward cost and contribution after reward, then reviews/edits an AI-assisted programme proposal before explicit save/publish. | Published reward truth uses the reviewed rule; customer never sees internal cost/margin. | `SPA-GLOW` Glow serum SGD 68 / cost SGD 24 and Spa Ritual 60 SGD 60 with explicit delivery-cost assumption. | Missing/zero/negative cost, price below cost, sparse history, stale catalogue, denied role, AI unavailable, retry/draft conflict, mobile. | `db/tests/v122_owner_seven_workflows.sql` verifies the fail-closed bigint cost contract; V122 arithmetic proves SGD 44 / 64.7% and editable post-reward contribution; desktop evidence `owner-grow-desktop-1440.png`. | `VERIFIED_LOCAL` |
| `CUSTOMER-CATEGORY-001` | Business sector/category derives from governed sector metadata, not customer input. | N/A | Customer’s already-linked programmes group into Personal care, Food & drink, Fitness and Other; selecting a card opens the same programme. | `CUS-MEI` linked to `SPA-GLOW`, `CAFE-HARBOUR`, `FIT-NORTHSTAR`, plus unknown-sector firm. | Zero/one/many, missing logo, unknown category, duplicate display names, selected-card return, slow/error/retry, keyboard and 390px. | `tests/business-ui/v122-owner-seven-workflows.test.mjs`: category mapping/grouping; real-browser 390px evidence `docs/qa/evidence/v122-browser/customer-categories-promo-mobile-390.png` records `clientWidth=scrollWidth=390`. | `VERIFIED_LOCAL` |
| `FEEDBACK-SYNC-001` | Owner configures a verified Google review URL and Feedback permissions for the firm. | Authorized owner/manager sees newly submitted customer feedback after reload; another tenant and denied staff cannot read it. | Customer submits 1–5 stars once; after internal persistence every score receives the same optional configured Google review link. Five-star copy may be warmer, but link reachability never depends on score. Missing/invalid URL and failed writes never redirect. | `SPA-GLOW`, `CUS-MEI`, configured Google Business review URL; `CAFE-HARBOUR` comparison tenant. | Same-key retry, lost response, double tap, reconnect, malformed/missing URL, denied role, cross-tenant, empty/error state, desktop/390px. | `tests/business-ui/v122-owner-seven-workflows.test.mjs`: “feedback emits a staff refresh signal and public-review access is score independent”; disposable branch verifies the feedback notification trigger and authenticated preference ACL. | `VERIFIED_LOCAL` |
| `I18N-001` | User selects English, Simplified Chinese, or Malay. | Same role continues on translated workspace. | UI changes language without duplicating merchant fields; customer remains English. | `SPA-GLOW` owner/manager routes. | Missing key, mixed language, reload/new device, long labels/mobile. | Local localization suite exists; all-route browser inventory pending. | `IMPLEMENTED_UNVERIFIED` |
| `BRAND-001` | Owner uploads permitted logo with description. | Staff sees current workspace identity. | Customer programme card shows the saved logo after reload. | `SPA-GLOW` square PNG/JPG/WebP plus invalid file. | Denied role, oversize/type, storage failure, retry, replacement/cache. | v102 local upload ownership regression passed; target storage/browser proof pending. | `VERIFIED_LOCAL` |
| `PACKAGE-001` | Owner creates Spa 30/60/90 variants and 5x Spa 60 package at $270 vs $300; chooses purchase points. | Staff sells package and later uses one eligible session in Quick Earn. | Customer sees discount, purchase history, 4/5 remaining, and correct points. | `SPA-GLOW`, `CUS-MEI`. | Wrong variant, wrong branch, used-up/expired, restore, retry, points off. | v102 focused UI and disposable DB contract passed; target browser/DB proof pending. | `VERIFIED_LOCAL` |
| `GIFT-001` | Owner disables new gift-card issuance while an existing SGD 50 gift remains. | Staff cannot issue a new card but can transfer eligible existing liability to the linked customer's programme credit. | Customer retains lawful value; the exact customer-history production projection remains a separate release proof. | `SPA-GLOW`, comparison branch, synthetic current customer and pre-existing SGD 50 liability. | Disabled issuance, insufficient/expired gift, partial transfer, same-key retry, reversal, wrong firm/branch. | `db/tests/v117_effective_loyalty_boundaries.sql` proves disabled issuance adds zero gift rows, the existing SGD 50 liability remains readable with Till+Clients, one SGD 10 transfer creates exactly one `gift_card_load` credit and one immutable keyed receipt while leaving SGD 40, and same-key replay creates no second economic or receipt row. `tests/customer-wallet/v102-package-gift-preference-ui.test.mjs` and the v117 static contract pass. Exact artifacts: `V117-EFFECTIVE-LOYALTY-BOUNDARIES-ACCEPTANCE.md`. Independent Sol review and authorized production customer-history proof remain. | `VERIFIED_DATABASE` |
| `NAV-001` | N/A | Role navigates left menu while editing/scrolling. | Route changes without blank screen, scroll jump, hard refresh, or lost state. | Owner/manager/front desk on `SPA-GLOW`. | Slow chunk, stale cache, offline/reconnect, back/forward, mobile menu. | Workspace load hotfix exists; route sweep evidence incomplete. | `IMPLEMENTED_UNVERIFIED` |
| `MOBILE-001` | User installs web app and grants selected permissions. | Staff/customer uses touch, camera, and foreground/background resume. | Layout, QR, passkey, notification, and state recovery remain usable. | iPhone-class 390px, Android-class 412px, desktop Chrome. | Denied camera/push, offline, rotation, keyboard, safe area, stale service worker. | PWA/wrapper docs and tests exist; device acceptance incomplete. | `IMPLEMENTED_UNVERIFIED` |
| `MOBILE-STORES-001` | Owner supplies Apple/Google teams, signing and store metadata; `app/` remains the only web source; production Turnstile allows the exact packaged hostname `localhost`. | Signed iOS/Android candidates authenticate from fixed allowlisted WebView origins and complete links, QR, permissions, resume and offline/reconnect journeys. Every copied/shared/QR/invite/auth-email link resolves under `https://www.nestly.asia`, never the local WebView; every bundled entrypoint enforces the restrictive CSP. | Store consoles accept signed artifacts; physical acceptance records build IDs, Turnstile/auth/gateway success and no wrapped-website-only or broken-link risk. | `SPA-GLOW`, `CUS-NEW`, iPhone-class device, current Android device, desktop fallback. | Missing/hostile origin, missing Turnstile hostname, wrong action/hostname token, local-origin share/auth callback, absent CSP on any entrypoint, signing gaps, wrong deep link, denied permission, expired session, offline cold start, safe areas and target policy. | Local regressions/mobile sync/full gate pass. Production gateway smoke proves exact native-origin allow/hostile denial; both live entrypoints expose CSP and canonical links. Signed builds, production Turnstile `localhost` configuration and physical-device/store journeys remain. Evidence: `V125-NO-GST-NATIVE-READINESS.md`. | `IMPLEMENTED_UNVERIFIED` |
| `NOTIFY-001` | Business enables eligible transactional events; platform controls promotions. | Staff event creates one notification. | Correct customer receives one timely message with safe deep link. | Package expiry, points, booking, redemption fixtures. | Permission denied, duplicate event, timezone boundary, 3-day/1-day scheduler. | Basic notification work exists; delivery/evidence matrix incomplete. | `IMPLEMENTED_UNVERIFIED` |
| `PROMO-001` | Owner creates a company-wide structured promotion draft, optionally polishes wording without API credits, uploads an owned image, reviews exact dates/terms/CTA, and publishes within the company's finite first-publication entitlement. Super Admin controls entitlement and can read/support an exact existing receipt replay, but cannot create or change fresh promotion content. | Manager/front desk may see ordinary programme operations but cannot publish, replace promotion media, or use the legacy v95 offer writer. | Selected programme shows at most two currently active, image-backed, global promotion cards before supporting catalogue content; brand/points header stays compact and CTA has one clear outcome. | `SPA-GLOW`: 30% off Spa Ritual 60 through 30 Aug 2026 for National Day, plus 10 first-published, expired, future, and deliberately rejected non-null-branch offers. | Empty image, invalid type/size, maximum accepted copy lengths, invented-fact prevention, draft, future, expired, quota 10/11, window ended, denied role, Super Admin fresh-write denial/exact replay, legacy-writer bypass, non-null branch, unlinked customer, failed upload, lost response, tenant switch, priority `0`, unpublish/retry/refresh, long unbroken copy, modal fallback, 390px/412px. | 32 focused v104 application/database tests and the complete 1,034-test repository validation pass; quality, runtime-config, manifest, canonical-order and build gates are green. The maximum-bound regression proves accepted 140-character offer and 80-character occasion inputs produce a saveable headline while retaining exact facts/date/CTA in the message. The date regression proves valid Singapore inputs reach exact database instants. Disposable PostgreSQL behavioural SQL proves owner-only fresh authoring, zero-mutation Super Admin denial, Super Admin exact-receipt support replay, company-wide/non-null-branch rejection, lifetime quota, exclusive cutoff, date/media filtering, max-two sync, stable receipt replay and non-destructive rollback. A real two-session race proves exactly one of two contenders consumes slot 10 and creates one receipt. Deterministic production-render evidence in `docs/qa/evidence/V104-PROMOTION-VISUAL-ACCEPTANCE.md` records two cards, white headline contrast, full wrapping in cards and details modal, 44px CTAs and no overflow at 1440px, 390px and 412px. Authenticated target owner-upload/customer-refresh evidence, authorized migration/release, and production smoke remain pending. | `VERIFIED_LOCAL` |
| `BILLING-001` | Platform configures plan, representative, and grace policy. | Billing events update firm state automatically. | Owner sees paid/next date or overdue countdown; access pauses day 14 and recovers after payment. | Quarterly/half-yearly/annual firms. | GST, refund, chargeback, failed webhook/replay, staff departure, renewal year. | Platform billing work exists; provider event campaign incomplete. | `IMPLEMENTED_UNVERIFIED` |
| `BILLING-PRICING-001` | Platform activates matching SGD monthly and annual base/capacity Stripe Prices; annual is the owner-facing default, includes 1,000 customer profiles, and the UI displays the exact module inclusion/exclusion list and the SGD 168 comparison price without representing it as a charge. Staff accounts are included rather than counted for billing. | Owner chooses monthly or annual and a capacity in exact 1,000-profile increments, confirms the exact recurring total, completes Stripe Checkout, and may later request an increase. V124 exposes no self-service decrease through either capacity or cadence commands. The same selection reuses one session-stored request key and command after lost responses; terminal failed/canceled commands render failure, not submission success. Stripe event/invoice truth remains authoritative. | Owner sees SGD 149 monthly or SGD 1,188 annual, exact capacity/add-on arithmetic, paid/next date, and one immutable 30-day request deadline derived from the earliest successful paid invoice for the matching V124 provider subscription. A prorated change stays uncertain while Stripe has `pending_update`, exact recovery remains allowed after the webhook projects equal requested capacity, and the command retains its snapshotted catalogue row across later price rollovers. A complete newer subscription snapshot removes locally retained items that Stripe omitted. | `SPA-GLOW-BILLING`: 1,000 and 3,000 profile selections on both cadences. | Zero/999/1,001/current+ profiles; invalid increment; forged amount/price/capacity; no active catalogue; catalogue rollover during recovery; monthly/annual switch with lower capacity; GST unset; duplicate click; lost response/replay; terminal failed/canceled command; pending payment/SCA before and after webhook projection; removed capacity item; legacy renewal and historical backfill; invoice failure/refund/chargeback; cancel/rejoin must not reset guarantee; customer portal/reconnect; 390px. | `tests/billing/v124-stripe-pricing.test.mjs` (12/12); `db/tests/v124_stripe_launch_pricing.sql` on fresh PostgreSQL 17 after 164/164 migrations; full gate 1,339/1,339 plus build; public desktop/390px policy browser checks. Sol independently accepted the exact corrected local candidate after regression-first closure of the provider-transition findings. Authenticated checkout and provider/production proof remain. Evidence: `V124-STRIPE-LAUNCH-PRICING-ACCEPTANCE.md`. | `VERIFIED_LOCAL` |
| `BILLING-TAX-001` | Super Admin activates only provider-verified explicit-`exclusive` catalogue rows; policy records no GST; migration preflight aborts if historical billing tax exists. | Checkout and existing-subscription changes/recovery clear and verify automatic tax, default tax rates and every item tax rate; tax normalization is separate from Stripe pending updates, pending payloads contain no unsupported tax fields, and a fresh provider retrieve must remain tax-free and exactly match requested price/quantity/cancel state before completion; webhook/reconciliation projects invoice truth. | Owner/platform show `GST not charged`, SGD 0.00 and total=subtotal. Invoice-first delivery is rejected before V124 terms. Day 13 remains open, day 14 pauses only owner workspace, and newer payment restores it without deleting customer value. | `SPA-GLOW-BILLING`, `PLAT-OVERDUE-01`, `PLAT-OVERDUE-14`, `CUS-MEI`. | Inclusive/forged Price; automatic/default/item tax; unsupported pending payload; external tax or requested-state drift after idempotent mutation replay; invoice-before-subscription; stale/duplicate event; historical-tax preflight; day boundary; roles; provider timeout/lost-response recovery; refresh/reconnect. | Local 37/37 focused, 49/49 combined, 1,348/1,348 plus build and 165/165 DB replay pass. Production migration proves zero tax, five constraints/four triggers, and live UI/Edge smoke passes. Active catalogue rows remain zero; authenticated live Stripe checkout/webhook/reconciliation proof remains. Evidence: `V125-NO-GST-NATIVE-READINESS.md`. | `VERIFIED_LOCAL` |
| `LEGAL-IDENTITY-001` | Governing owner supplies current ACRA identity and monitored email; formal DPO appointment remains a separate launch gate. | Staff and platform users encounter the same legal operator in commercial/help surfaces; no identity-document payload is stored. | Terms, Privacy, data-request and subscription surfaces show NESTLY TECHNOLOGIES PTE. LTD., UEN 202634502E and `nestlyasia@gmail.com`; no director NRIC appears. | Public legal/contact pages and one synthetic subscription order. | Stale prior email, inconsistent entity, mobile wrapping, dead mailto, source PDF accidentally staged, national-ID scan. | Exact V124 legal manifests, public-surface regressions, repository scan, and desktop/390px browser checks pass locally. Counsel/DPO, live mailbox, Sol, release and production evidence remain. Evidence: `V124-STRIPE-LAUNCH-PRICING-ACCEPTANCE.md`. | `VERIFIED_LOCAL` |
| `PROMO-PUSH-001` | Owner publishes an eligible company-wide promotion and transactional expiry policies remain enabled; customer linkage and consent are authoritative. | Publisher sees publication truth only; delivery workers enqueue/retry with stable event/customer/channel keys and do not claim success without a provider receipt. | Linked customer receives one in-app prompt for a newly active offer and, when permission/keys exist, one Web Push with safe programme deep link; expiring value produces the existing 3-day/1-day notices once each. | `SPA-GLOW` National Day Glow; `CUS-MEI`; expiring package/value fixtures at Singapore boundaries. | Unlinked/opted-out customer, denied browser permission, missing VAPID/dispatcher configuration, inactive/future/expired offer, duplicate job, provider failure/backoff, reconnect, 390px. | `db/tests/v122_owner_seven_workflows.sql` verifies explicit topic/source eligibility, zero enqueue for missing content, authenticated ACL and one expiry cron; V122 regression covers dedupe; 390px popup/dismiss evidence is `customer-categories-promo-mobile-390.png`. | `VERIFIED_LOCAL` |
| `READINESS-001` | The effective sector, firm, branch, and staff module projection governs the exact destinations and actions exposed for each role. Configuration/detail pages keep advanced writers secondary to the user's immediate task. | Owner, manager, front desk, Super Admin, configurable Admin, and assigned/unassigned Sales/Consultant traverse every permitted destination; enabled controls invoke one named handler, async writers prevent duplicate submission and recover visibly, destructive actions confirm, and read-only/disabled/denied paths expose no callable writer. | Customer and dual-role users traverse every projected programme destination; enabled actions prepare or persist the labelled outcome once, while missing configuration, insufficient eligibility, denied permission, and load failure show specific non-deceptive states with recovery only when retry is meaningful. | `SPA-GLOW` Orchard/Tampines with Olivia, Maya, Farah, Chen, Aisha, duplicate Lee customers and `CUS-MEI`; `CAFE-HARBOUR`; `FIT-NORTHSTAR`; platform pending/won/overdue fixtures. | Zero/one/many, enabled/read-only/disabled/unconfigured, wrong branch, forged direct route, stale persona, slow/error/retry, double tap, refresh/back/reconnect, desktop/390px, keyboard/touch, long labels and outer overflow. Production checks are read-only. | Baseline `31331e71640d3b25c63b3b85d7b23d9fda5e63a8`. `tests/quality/v123-module-button-readiness.test.mjs` passes 9/9, the complete gate passes 1,327/1,327 plus build, and `module-button-readiness.mjs` inventories 25 business routes, 9 platform routes and 269 literal controls. Focused production-component Chrome evidence covers sign-in, Setup copy recovery, semantic staff keyboard routing, pending-scope invalidation, explicit rejected-request Retry/success, scoped P&L KPI exports, one-RPC bundle busy/recovery, 1440px/390px and outer overflow. Disposable PostgreSQL 17 applies 163/163 canonical migrations; `db/tests/v123_module_button_readiness.sql` proves atomic persistence, exact replay, edited-key/cross-tenant/read-only denial and rolls back cleanly. Sol 5.6 independently accepted this limited local patch with no remaining code blocker. Evidence: `V123-MODULE-BUTTON-READINESS.md`. These focused artifacts do not satisfy the row's required authenticated target all-role/module sweep; `RELEASE-001` remains `CAPTURED` and production release is stopped. | `VERIFIED_LOCAL` |
| `RELEASE-001` | Owner and reviewer define release scope. | Full role/button sweep uses the exact candidate. | Only evidenced requirements are reported complete. | All named fixtures. | Disabled, empty, denied, retry, refresh, mobile, branch, concurrency. | This repository evidence system is now defined; execution remains open. | `CAPTURED` |
| `ROLLBACK-COMPAT-001` | Approval-first activation or an explicit already-approved synthetic workspace; provider invoice state precedes its paid event; snapshot attention remains fail-closed; protected media is never deleted through SQL. | Owner/front desk/bookkeeper/platform actors invoke historical RPCs under intended permissions and explicit branch assignments. | v92/v94/v95 and v106-v114 pass against the final chain without weakening approval, Storage, module, finance, commerce, retention, tenant, or branch controls. | Approval-first pristine A/B tenants; overdue v94 invoice; v95 present and deliberately absent owned media paths; sector-specific approved v106-v114 businesses. | Unapproved legacy creation, stale provider events, nullable/normalized attention, existing Storage object, non-owner/non-finance, outsider/cross-firm/wrong-branch, duplicate request/source, retry and backoff. | Local evidence 2026-07-30: v92/v94/v94-null-guard/v95 and v106-v114 all exit 0 under `psql -X -v ON_ERROR_STOP=1` and end in `ROLLBACK`; `v95_customer_web_push_concurrency.sh`, `v111_customer_identity_governance_concurrency.sh`, `v112_concurrent_replay_delivery_backoff_concurrency.sh`, `v113_effective_identity_consumers_concurrency.sh`, and `v114_traceable_growth_costs_concurrency.sh` all emit PASS; the harnesses restore temporarily enabled v108/v109 flags to `false`; `node --test tests/platform/v113-effective-identity-consumers.test.mjs` passes 9/9 and asserts the typed JSON candidate projection with no static `pg_temp` relation; `supabase db lint --local --level error` returns `{"results":[]}`. The v113 source/mirror migration pair is byte-identical. No production migration, grant, RLS policy, data, or secret was changed. | `VERIFIED_LOCAL` |

## Per-change expansion template

Copy this below the working matrix or into a phase evidence document before
implementation:

| Field | Required entry |
| --- | --- |
| Requirement ID | Existing ledger ID or a new stable ID |
| Source | Owner message date, screenshot description, audit item, or defect |
| Exact acceptance | Observable UI plus persistent data result |
| Owner configuration | Toggle, catalogue, branch, programme, permission |
| Staff operation | Role, action, confirmation, correction |
| Customer projection | UI/history/balance/notification result |
| Data contract | Tables/RPCs/events and idempotency key |
| Fixture | Named fixture and concrete values |
| Negative states | Disabled/empty/denied/retry/refresh/mobile/branch as applicable |
| Regression | Exact test file and test name |
| Browser evidence | Artifact path, viewport, role, candidate hash |
| Database evidence | Environment, query/test artifact, before/after values |
| Reviewer | Independent reviewer and verdict |
| State | One ledger lifecycle state |
